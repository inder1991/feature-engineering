# SP-2 Intake + Clarification + Human Gate #1 — Implementation Plan (Overview)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking. The **Global Constraints**, the **Additive SP-0 surface**, and the **Shared Contract — Key signatures** below are AUTHORITATIVE; every phase file references them and must not redefine or drift a shared symbol. When a signature here and a phase file disagree, THIS FILE wins.

**Goal:** Build Layer 1 (Intake & Normalization) and Layer 2 (Contract control + human clarification, Human Gate #1) of the platform — architecture §3:88–106 + the [SP-2 design spec](../architecture/2026-07-01-sp2-intake-clarification-design.md) — as a thin domain layer over the existing `featuregen` package. SP-2 turns a data scientist's plain-English intent into a **Confirmed Feature Contract** (the first executable-eligible artifact) via an **auditable LLM** that *structures/suggests*, deterministic checks that *validate/enforce*, a human who *confirms business meaning*, and the registry that *governs*. SP-2 is the **first sub-project that invokes an LLM** and establishes the platform's permanent **auditable-LLM surface**.

**Architecture:** No new aggregate and no event-store aggregate-CHECK migration (unlike SP-1) — the whole Layer-1/2 flow rides SP-0's existing **`run` aggregate**, its DRAFT → CONFIRMED_CONTRACT run-states (SP-0 §4.3), its **staged-document DAG** (Draft / Assumption-Ledger / Confirmed as frozen content-hashed documents), the **`CLARIFICATION`** human-gate, the document **`PRIMARY_SELECTED`** candidate-promotion primitive, the durable runtime, identity/authz + structural SoD, and the security-audit stream. SP-2's additions are **additive registrations + handlers** — event-types, three document content-schemas + upcasters, an SP-2-owned append-only `llm_call` record store, one backward-compatible human-gate/park-reason migration, one additive service authz row, and a request-owner guard SP-0 does not provide. The new package is `src/featuregen/intake/`.

**Tech Stack:** Python 3.11 (uv), PostgreSQL, psycopg 3, jsonschema, pytest (+ pytest-postgresql, pytest-timeout). Lint/format: ruff. The **LLM adapter is config-gated**: `FakeLLM` is the deterministic CI default (no network); the real Anthropic-SDK Claude adapter (`anthropic`, default model `claude-opus-4-8`, adaptive thinking, structured outputs) is shipped but **never required in CI**. No other new runtime dependencies.

---

## Global Constraints

- **Python ≥ 3.11**, package `featuregen` under `src/`; SP-2 code lives in `src/featuregen/intake/`, tests under `tests/featuregen/intake/`. No new *required* runtime deps (the `anthropic` SDK is optional / config-gated, imported only inside the real adapter, never at module import time — CI never imports it).
- **TDD per task:** failing test → watch fail → minimal code → watch pass → commit. Every task ends green. All tests run on **`FakeLLM`** — hermetic, reproducible, no network, required in CI (§15). The real adapter is exercised only by an opt-in, config-gated smoke test never gated in CI.
- **Authority model (verbatim, load-bearing):** **the LLM structures/suggests · the platform validates/enforces · the human confirms · the registry governs.** The LLM (`service:intake-agent`) may propose the structured Draft, field scores, candidate calculations, and clarifying questions; it may **never** decide anything is final, write a Confirmed contract, or see raw data/PII. Deterministic platform code validates the content-schema, runs the banking/prohibited-intent screens, runs Minimum Contract Validation, and records every assumption. A **human requester** confirms meaning / picks a candidate / edits fields at Gate #1. The registry governs from Gate #2 (SP-5) onward.
- **Never executable until CONFIRMED (the hard invariant SP-2 exists to protect):** *no Confirmed Feature Contract → no mapping, no compilation, no execution* (design §3:106). A Draft (`status: NEEDS_CLARIFICATION`) is never executable; a run with a **non-empty `open_fields` can never pass Gate #1** (guard `open_fields_empty`).
- **No PII / no data values to the LLM (three-point boundary):** the LLM sees **only** (a) the intent as **redacted, LLM-safe text** from SP-2's `IntentRedactor` seam, and (b) **catalog metadata** — object/column **names, types, asserted grain**, plus any catalog-*declared* enum/code metadata (never profiled values, rows, samples, extrema, or overlay metrics). Enforced at three points with a clean ownership split — **SP-0 classifies** at ingest (`raw_input_classification`), **SP-2 redacts** (`IntentRedactor`, fail-closed on un-redactable / `unscanned`), **SP-2 guards egress** (pre-send hard-fail on data values / un-redacted PII → security-audit). The registered output-schemas are **structural-only** — no PHI/PII in property names, enums, `const`s, or descriptions (server-compiled + cross-call-cached artifact).
- **Event-sourced audit for every LLM call:** each `LLMClient.call` writes **one immutable record** to the SP-2-owned append-only `llm_call` store and emits an `LLM_CALL_RECORDED` event on the run. The record stores the **redacted input itself** (`redacted_input`, replayable — not hash-only), plus provider/model/prompt+schema versions/generation-settings/redaction-version/input-hash/raw-output/validation-result/repair-attempts/latency/cost. Idempotency reuses a record **only** on a **full-identity** key match (below). The store is classified **sensitive / governance-retained / read-controlled**.
- **Fail-closed everywhere:** a malformed LLM structure is a **doubt, not a value** (bounded repair → fail into clarification); a refusal fails into clarification directly (not repair); truncation / timeout / 429 / transient 5xx → bounded retry; non-retryable 4xx → fail closed (auth failures security-audited); an un-redactable-PII intent fails into the manual path (no payload dispatched); a prohibited intent blocks; an exhausted refinement loop auto-parks; an unavailable/unversioned `BankingDomainCatalog` parks. No outcome is silently swallowed, guessed, or executed on. **No silent production fallback:** an enabled-but-unavailable real adapter fails closed into clarification — it never swaps in `FakeLLM`.
- **`FakeLLM` is the default; the real adapter is never required in CI** (Decision D5). All agent code depends on the **`LLMClient` interface — never on a provider**; the provider is isolated to the adapter.
- **Foundation: SP-0 only, + read-only `BankingDomainCatalog`.** SP-2 **builds on SP-0 only** and **reads** two reference surfaces without building them: SP-1's merged-view read API (`resolve_fact` / `list_objects`) for names/types/grain during normalization *only* (never grounding), and the **SP-0-governed, read-only `BankingDomainCatalog`** for the intake banking-boundary + blocked-class + sensitive-proxy screens *only* (never grounding/execution). Reading reference data does not violate "SP-0 only" (Decision D8, ratified). All policy-aware VERIFIED-fact grounding is SP-3.
- **Append path & registration (SP-0 discipline, unchanged):** never INSERT into `events` directly — use `featuregen.aggregates._append.append(...)` (keyword-only; OCC + provenance + `global_seq`). New streams use `expected_version=0`. **Every new event type MUST be registered** (schema_version=1) before any append or `append_event` raises `SchemaValidationError`. **No inline PII** in event payloads (`assert_no_inline_pii` runs on append; `privacy/classification.py:70`) — sensitive bodies are referenced (`blob_`/`doc_`), never inlined.
- **Migrations are additive & lexically ordered** (`src/featuregen/db/migrations/*.sql`, LEXICAL order). SP-2's single DDL change (the gate/park CHECK widening) is a new `.sql` file **`0508_use_case_onboarding_gates.sql`** — it sorts **after** `0070` (gates) and after SP-1's `0504`–`0507`, and **before** `0810` (privacy); it uses `ALTER TABLE ... DROP/ADD CONSTRAINT`, is idempotent (guarded), and **changes no existing row and adds no new aggregate** (so it is NOT an event-store aggregate-CHECK migration). SP-1's `0505_overlay_gates.sql` is the precedent.

---

## The additive SP-0 surface SP-2 registers (§2.1 — all backward-compatible)

All five are additive; **no existing SP-0 row, document, event, or aggregate is rewritten.** (Precedent: SP-1's additive `0505_overlay_gates.sql` gate-CHECK widening.)

1. **Event types** (registered in SP-0's event-type registry, schema-owned by SP-2, emitted on the `run` aggregate): `INTENT_SUBMITTED`, `DRAFT_CONTRACT_PRODUCED`, `CONTRACT_CRITIQUED`, `FIELD_AUTO_RESOLVED`, `CLARIFICATION_REQUESTED`, `CLARIFICATION_ANSWERED`, `CONTRACT_REFINED`, `MINIMUM_CONTRACT_VALIDATED`, `CONTRACT_CONFIRMED`, `USE_CASE_ONBOARDING_REQUESTED`, `INTENT_REJECTED` (carries `OUT_OF_SCOPE` | `PROHIBITED_DATA_CLASS` + catalog `version`), and `LLM_CALL_RECORDED`.
2. **Document content-schemas** for `DRAFT_CONTRACT`, `ASSUMPTION_LEDGER`, `CONFIRMED_CONTRACT` registered in SP-0's document registry (versioned, with total chained reader-upcasters). The **stage enum is NOT extended** — `contracts/documents.py::Stage` already publishes all three (`DRAFT_CONTRACT`, `ASSUMPTION_LEDGER`, `CONFIRMED_CONTRACT`).
3. **The `llm_call` immutable record store** — a new SP-2-owned append-only table (an SP-0-style write-once artifact, like SP-1's `overlay_evidence`), referenced by `llm_call_ref`, classified **sensitive / governance-retained / read-controlled**. Not an event aggregate; needs no CHECK change.
4. **One additive `authz_policy` row (rejection authority)** — because SP-0's `reject` is **validator-only** (`authz/policy.py:42`), SP-2 registers `("reject_intent","","intake-agent","service",None)` admitting the platform/service principal to issue the deterministic intake-rejection terminal outcome `reject_intent` (→ SP-0 `RUN_REJECTED`). Plus the SP-2 command-capability rows (below). This **adds** rows and **changes no existing SP-0 row** — the validator-only `reject` (`authz/policy.py:42`) and data-scientist-owned `withdraw` (`authz/policy.py:41`) are untouched; requester-initiated abandonment reuses that existing `withdraw`, not `reject`.
5. **One additive human-gate + park-reason migration** (`0508_use_case_onboarding_gates.sql`) — widens SP-0's `human_tasks` gate CHECK (`0070`: `CLARIFICATION`/`DATA_STEWARD`/`COMPLIANCE`/`INDEPENDENT_VALIDATION`/`FINAL_APPROVAL`) with a new **`USE_CASE_ONBOARDING`** gate value, and registers the **`NEEDS_USE_CASE_ONBOARDING`** park hold-state as an additive `RUN_PARKED` park-reason (base `RUN_PARKED` payload carries only `owner`/`waiting_on_fact`, `run_lifecycle.py`). Only widens a CHECK / adds allowed values — not an aggregate-CHECK migration.

**The request-owner guard (SP-2-built; SP-0 does NOT provide it).** SP-0 admits the `data_scientist` role for *any* human (`authz/policy.py`), and `gates/tasks.py::submit_human_signal` checks the responder's **role/scope/quorum** — **never** that the acting `subject` is in the task's `eligible_assignees` (verified: eligibility reads `eligible.get("role"|"scope")`, not the subject). So role-authz alone would let *any* `data_scientist` answer/confirm another author's task. SP-2 therefore adds an explicit guard in its own handlers: `actor_is_request_owner` (at `answer_clarification`) and `confirmer_is_requester_human = actor_is_request_owner ∧ actor_kind=="human"` (at `confirm_contract` / `request_edit` / Gate #1). A mismatch is **denied + written to the security-audit stream**, never counted. In addition, the clarification and Gate #1 tasks SP-2 opens set **`delegation_allowed=False`** (SP-0's `GateTaskSpec.delegation_allowed` **defaults to `True`**, `contracts/envelopes.py:209`) — the subject guard alone is necessary but not sufficient, since a delegate could otherwise stand in.

---

## Shared Contract — Key signatures (authoritative — every phase uses these exact symbols)

### New package layout (`src/featuregen/intake/`)
| File | Responsibility |
|---|---|
| `contract.py` | Draft/Confirmed/Ledger content-schemas + closed-enum vocabularies + `UNKNOWN` reuse; the tagged `calculation_method` shape; `validate_semantics(...)`; `assemble_confirmed(...)` (Draft→Confirmed renames + method reshape); reader-upcasters; `register_contract_schemas(registry)` |
| `banking_catalog.py` | `BankingDomainCatalog` reader (read-only) + the deterministic `classify_intent(...) -> IntakeClassification` + `IntakeOutcome` enum |
| `redaction.py` | `IntentRedactor` Protocol + `DefaultIntentRedactor`, `RedactionResult`; the egress guard `assert_llm_safe(request)` |
| `llm.py` | `LLMClient` Protocol, `LLMRequest`, `LLMResult`, `FakeLLM`; the structured-output + bounded-repair/retry taxonomy; `call_llm(conn, client, request, *, run_id, actor)` (event-sourced wrapper); the `llm_call` record store (`record_llm_call`, `read_llm_call`) + idempotency key |
| `llm_claude.py` | The config-gated real Claude adapter (Anthropic SDK) — imported lazily, never in CI |
| `scoring.py` | per-field ambiguity/confidence scoring (LLM self-report + deterministic catalog-cardinality check, cautious-max combine) |
| `doubt_router.py` | the deterministic Doubt Router (`route_field(...) -> "auto"|"human"`); config-gated thresholds |
| `critique.py` | the Critique `CONTRACT_REVIEW` mode (challenger-only; feeds the Doubt Router) |
| `candidates.py` | `CandidateGenerator` Protocol, `Candidate`, `StubCandidateGenerator` |
| `mcv.py` | Minimum Contract Validation checklist (`minimum_contract_validated(...)`); SP-2 lifecycle guards |
| `commands.py` | handlers `submit_intent`, `answer_clarification`, `select_candidate_doc`, `open_gate1_task`, `confirm_contract`, `request_edit`, `reject_intent`; the `_SP2_CATALOG` tuple; `register_sp2_commands()` (idempotent) |
| `events.py` | SP-2 event-type constants + `SP2_EVENT_SCHEMAS` + `register_sp2_event_types(registry)` |
| `read_model.py` | `get_contract(conn, run_id) -> ContractView` (service-internal, for SP-3) |
| `bootstrap.py` | `register_sp2(handler_registry)` (event schemas + contract schemas + commands + `PRIMARY_SELECTED`) + `seed_sp2_authz(conn)` (authz rows + `llm_call`/checkpoint init) |

### SP-0 symbols SP-2 reuses verbatim (do NOT re-implement)
- **Append / OCC:** `featuregen.aggregates._append.append(conn, *, aggregate, aggregate_id, type, payload, actor, run_id=..., request_id=..., expected_version=..., caused_by=...) -> EventEnvelope`.
- **Run lifecycle (`aggregates/run_lifecycle.py`):** `reject_command` → `RUN_REJECTED`; `withdraw_command` → `RUN_WITHDRAWN`; `park_command` → `RUN_PARKED` (payload `{run_id, owner, waiting_on_fact}`); `unpark_command`; `run_is_terminal(conn, run_id)` (terminal ∈ `RUN_REJECTED`/`RUN_CANCELLED`/`RUN_WITHDRAWN`). **SP-2 reuses `withdraw` for requester abandonment; SP-2 NEVER invokes SP-0's validator-only `reject`.**
- **Human gate (`gates/tasks.py`):** `open_task(conn, spec: GateTaskSpec, actor) -> task_id`; `submit_human_signal(conn, task_id, *, response, actor, expected_task_version, on_behalf_of=None) -> SignalResult` (OCC on `expected_task_version`; role/scope/quorum only — **not** subject membership); `cancel_task(conn, task_id, *, reason, new_status="cancelled"|"superseded")`; `cancel_tasks_on_run_advance(conn, run_id, ...) -> int` (used by `open_gate1_task` to cancel pending per-field tasks). `GateTaskSpec` (`contracts/envelopes.py`): `gate, required_inputs, eligible_assignees, allowed_responses, run_id=None, feature_id=None, quorum_required=1, quorum_of_role=None, delegation_allowed=True, sla=None, ...` — **SP-2 always passes `delegation_allowed=False`** for its tasks.
- **Document candidate promotion (`documents/primary.py`):** `new_primary_selected(*, run_id, stage, doc_id, actor, provenance, caused_by=None) -> NewEvent` (payload `{doc_id, stage}`, aggregate `"run"`); `register_primary_selected(conn)`; `current_primary(conn, run_id, stage)`. This is the **document-level** primitive SP-2 uses for Gate #1 candidate selection — **NOT** the request-level `select_candidate` (`request_aggregate.py::select_candidate_command`, which promotes *run* candidates on a *request* stream — the wrong granularity).
- **Draft envelope (`documents/draft.py`):** `RAW_INPUT_CLASSIFICATIONS = ("contains_pii","clean","unscanned")`; `INTAKE_MODES = ("hypothesis","definition")`; `UNKNOWN = "UNKNOWN"` (sentinel; any `UNKNOWN`-valued field MUST be in `open_fields`); `validate_draft(body)` (envelope + required-field presence — semantic validation is SP-2's); `draft_has_open_fields(body)`; `register_draft_schemas(registry)`; `ASSUMPTION_LEDGER_JSON_SCHEMA` (`required: ["request_id","assumptions"]`, each item `required: ["field","value","rationale"]`).
- **Stage enum (`contracts/documents.py`):** `Stage.DRAFT_CONTRACT` / `Stage.ASSUMPTION_LEDGER` / `Stage.CONFIRMED_CONTRACT` (already published — not extended); `BRANCH_ROLES = ("candidate","primary","rejected","repair")`; `BODY_CLASSIFICATIONS = ("pii-erasable","governance-retained")` (contract bodies + ledger are `governance-retained`).
- **Authz (`authz/policy.py`):** `seed_authz_policy(conn)`; existing rows SP-2 relies on — `("withdraw","","data_scientist","human",None)` (:41), `("reject","","validator","human",None)` (:42, **not used by SP-2**), `("create_request"/"create_run","","intake-agent","service",None)`, `("submit_human_signal","CLARIFICATION","data_scientist"|"intake-agent",...)`, `("park","","data_scientist","human",None)`.
- **Command registry (`commands/registry.py`):** `register_command(action, handler)` (raises on duplicate → `register_sp2_commands()` is idempotent, skipping already-registered actions); `get_command`, `clear_registry`. Ingest classification guard: `privacy/classification.py::assert_no_inline_pii(payload)` (:70 — a classification guard, **not** a redactor).

### SP-2 command actions (registered; each handler `(conn, cmd) -> CommandResult`, args read from `cmd.args`)
| Action | `cmd.args` (key inputs) | Effect / guards |
|---|---|---|
| `submit_intent` | `request_id, intent_text, intake_mode, product?, region?` | `create_request` + `create_run` (run DRAFT); classify raw intent → `raw_input_classification`; hold raw in encrypted blob; `INTENT_SUBMITTED`; run `classify_intent` (§banking); redact → `call_llm(structure_intent)`; freeze Draft + Assumption-Ledger docs; hypothesis-mode → `CandidateGenerator.generate` → 1–3 candidate docs; `DRAFT_CONTRACT_PRODUCED`. Terminal outcomes route to `reject_intent`/`park`. |
| `answer_clarification` | `task_id, response, expected_task_version, answer` | **owner-guard** (`actor_is_request_owner`, else DENY + security-audit); `submit_human_signal(CLARIFICATION)`; `CLARIFICATION_ANSWERED`; drives the Refinement Loop (renormalize → rescore → re-route). |
| `select_candidate_doc` | `run_id, candidate_doc_id, stage` | document `PRIMARY_SELECTED` promotion (`new_primary_selected`) of the chosen candidate doc on the **run**; records only the chosen doc (write-once losers untouched). Invoked by `confirm_contract` in hypothesis mode; owner+human guarded. |
| `open_gate1_task` | `run_id` | only after MCV passes (guard `minimum_contract_validated`); opens the dedicated `CLARIFICATION`-gate confirm task (`allowed_responses=[confirm,edit,reject]`, `required_inputs=[final Draft ref]`, `eligible_assignees={owner}`, `delegation_allowed=False`); **cancels** pending per-field clarification tasks (`cancel_tasks_on_run_advance`). |
| `confirm_contract` | `run_id, task_id, expected_task_version, candidate_doc_id?, feature_name?, human_edits?` | Gate #1 `confirm`; **`confirmer_is_requester_human`** guard; re-run §8.4 prohibited-intent screen (authoritative block; version-drift re-check); hypothesis → `select_candidate_doc`; write `CONTRACT_CONFIRMED` + frozen Confirmed doc; DRAFT → CONFIRMED_CONTRACT. OCC on `expected_task_version`. |
| `request_edit` | `run_id, task_id, expected_task_version, field_edit` | Gate #1 `edit`; owner+human guard; supersede Draft with a **REVISED** version; re-run MCV; re-open a fresh gate task (or drop into the Refinement Loop if a field re-opens). OCC. |
| `reject_intent` | `run_id, reason, classification, catalog_version, matched_class?` | **platform/service-issued** terminal outcome for `OUT_OF_SCOPE`/`PROHIBITED_DATA_CLASS` → `INTENT_REJECTED` + SP-0 `RUN_REJECTED`; runs as `service:intake-agent` under the additive authz row. Requester abandonment instead reuses SP-0 `withdraw`. |

### SP-2 authz rows seeded (`seed_sp2_authz`; tuple `(action, gate, permitted_role, actor_kind, scope)`)
```
("submit_intent","","data_scientist","human",None)
("submit_intent","","intake-agent","service",None)
("answer_clarification","","data_scientist","human",None)
("select_candidate_doc","","data_scientist","human",None)
("open_gate1_task","","intake-agent","service",None)
("confirm_contract","","data_scientist","human",None)
("request_edit","","data_scientist","human",None)
("reject_intent","","intake-agent","service",None)   # additive rejection authority (§2.1 #4) — SP-0 `reject` stays validator-only
```
Fine-grained authority (request-owner; `confirmer_is_requester_human`; delegation-off) is enforced in `intake/mcv.py` guards + the command handlers, **not** in `_POLICY_ROWS` (mirrors SP-1). The `USE_CASE_ONBOARDING` gate answer is governance-owned and **out of SP-2 scope** — SP-2 only opens the task (`open_task`, existing `("open_task","","workflow","service",None)` row) + parks.

### `LLMClient` / `FakeLLM` / event-sourced envelope (§9.1–§9.3)
```python
class LLMClient(Protocol):
    def call(self, request: LLMRequest) -> LLMResult: ...

@dataclass(frozen=True)
class LLMRequest:
    task: str                 # "structure_intent" | "contract_review" | "generate_candidates" | "renormalize"
    prompt_id: str; prompt_version: int
    inputs: dict              # redacted intent text + catalog METADATA only — NO data values (§9.4)
    output_schema_id: str; output_schema_version: int   # registered, versioned, STRUCTURAL-ONLY (no PHI/PII)
    generation_settings: dict # provider/model + thinking/effort/max_tokens — pinned; part of the idempotency key

@dataclass(frozen=True)
class LLMResult:
    output: dict                 # parsed, schema-valid structured output (or the call FAILED, §9.2)
    self_reported_scores: dict   # per-field ambiguity/confidence the model reports (input to scoring)
    call_ref: str                # llm_call_ref → the event-sourced record
    status: str                  # "ok" | "repaired" | "retried" | "failed_into_clarification"

class FakeLLM(LLMClient):        # deterministic CI default: (task, prompt_id, input_hash) -> fixture output;
    ...                          # scriptable to invalid / refusal / ambiguous to exercise repair + Doubt Router

def call_llm(conn, client: LLMClient, request: LLMRequest, *, run_id: str,
             actor: IdentityEnvelope) -> LLMResult   # egress-guards, records the llm_call, emits LLM_CALL_RECORDED
```
- **Provider-failure taxonomy (each fail-closed):** valid → `ok`; invalid/unparseable/schema-invalid → **bounded repair** (default N=2, config-gated) → `repaired` or **fail into clarification**; `stop_reason=="refusal"` → **fail into clarification** (policy decline, not repair); `stop_reason=="max_tokens"` → **bounded retry**; schema-too-complex / compile-timeout → bounded retry then fail into manual; provider timeout / 429 / transient 5xx → **bounded retry with backoff**; non-retryable 4xx → **fail closed** (auth failures security-audited). Bounded retries reuse SP-0's durable-runtime hard-loop-limit primitive.
- **`llm_call` record fields:** `llm_call_ref, run_id, task, provider, model, prompt_id, prompt_version, output_schema_id, output_schema_version, generation_settings, redaction_version, input_hash, redacted_input, input_redaction, raw_output, validation_result, repair_attempts, latency_ms, cost_metadata, created_at, created_by`. Stores the **redacted input itself** (replayable), never the raw intent (that stays in SP-0's encrypted `raw_input_ref`).
- **Idempotency key (full call identity):** `(run_id, task, input_hash, provider, model, prompt_id, prompt_version, output_schema_id, output_schema_version, redaction_version, generation_settings)`. A retried *identical* call reuses its record (no double-charge); any provider/model/prompt/schema/redaction/settings change forces a fresh call.

### `IntentRedactor` seam + egress guard (§9.4)
```python
class IntentRedactor(Protocol):
    def redact(self, raw_intent: str, raw_input_classification: str) -> RedactionResult: ...

@dataclass(frozen=True)
class RedactionResult:
    text: str | None            # the ONLY LLM-safe rendering placed in LLMRequest.inputs; None ⟹ fail closed
    redaction_version: str      # stamped onto the llm_call record
    redacted_spans: tuple       # what was scrubbed → call record `input_redaction`
    disposition: str            # "ok" | "fail_into_clarification"

class DefaultIntentRedactor(IntentRedactor): ...   # default impl; fails closed on un-redactable PII / `unscanned`

def assert_llm_safe(request: LLMRequest) -> None    # egress hard-backstop: refuses `unscanned`, data values,
                                                    # or un-redacted PII → security-audit (hard failure, not a warning)
```

### `CandidateGenerator` seam (§7.1)
```python
class CandidateGenerator(Protocol):
    def generate(self, draft: DraftContract, catalog_metadata: CatalogView,
                 domain_context: DomainCatalogEntry | None) -> list[Candidate]: ...

@dataclass(frozen=True)
class Candidate:
    candidate_id: str
    definition_text: str        # plain-English, FeatLLM-style
    rationale: str              # one-line causal reasoning, surfaced at Gate #1
    calculation_method: CalculationMethod   # versioned, tagged (discriminated on `kind`) — SP-3 consumes deterministically
    signals: dict               # cheap, model-free plausibility/quality signals ONLY — NO IV/WoE/AUC (§7.3)
    provenance: dict            # llm_call_refs, generator_version

class StubCandidateGenerator(CandidateGenerator): ...   # ONE LLMClient call → 1–3 candidate docs; no router/memory/
                                                        # symbolic/diversity/few-shot (that is SP-12). Seam is stable SP-2→SP-12.
```

### Feature Contract content-schemas (registered in SP-0's document registry, versioned, upcasted)
- **Closed enum vocabularies** (extended only by a schema-version bump + upcaster): `intake_mode ∈ {definition, hypothesis}` (SP-0 `INTAKE_MODES`); `raw_input_classification ∈ {clean, contains_pii, unscanned}` (SP-0 `RAW_INPUT_CLASSIFICATIONS`); Draft `status = "NEEDS_CLARIFICATION"`; Confirmed `status = "CONFIRMED"`; `observation_intent.kind ∈ {point_in_time, as_of_event}`; `calculation_method.chosen.kind / considered[].kind ∈ {rolling_aggregate, point_snapshot, ratio, distribution_divergence}`; `field_scores.*.source / assumptions[].source ∈ {llm, default, catalog}`; `open_questions[].routed_to ∈ {human, auto}`.
- **`UNKNOWN` representation:** an unresolved semantic field carries the string sentinel `"UNKNOWN"` (SP-0 `documents/draft.py:10`) **and MUST be listed in `open_fields`** (`validate_draft` enforces). A **Confirmed** contract carries no `UNKNOWN` and an **empty** `open_fields`.
- **`DRAFT_CONTRACT` (`schema_version:1`)** — `required: [request_id, intake_mode, raw_input_ref, raw_input_classification, proposed_feature_name, feature_semantics, field_scores, open_fields, assumption_ledger_ref, provenance, status]`; `not: {required: ["raw_input"]}`; `additionalProperties: true` (SP-0 wraps the envelope). `feature_semantics` required: `[entity, entity_grain, observation_intent, calculation_method, windows, filters]` (+ optional `target_definition`). In the **Draft**, `calculation_method` is a **string label** (faithful method name or `"UNKNOWN"`). `field_scores.*` = `{ambiguity, confidence, source}`. `open_questions[]` = `{field, question, blocks_progress, routed_to, ...}`.
- **`CONFIRMED_CONTRACT` (`schema_version:1`)** — `required: [feature_name, intake_mode, raw_input_ref, raw_input_classification, entity, entity_key, feature_grain, observation_intent, calculation_method, assumption_ledger_ref, requires_independent_validation, confirmation, provenance, status]`; `additionalProperties: true`. **Draft→Confirmed renames** (deterministic, applied by the confirm step): `entity_grain` → `feature_grain` (+ derived `entity_key`); `proposed_feature_name` → `feature_name`. The Draft string `calculation_method` (+ `windows`/`filters`) is **reshaped** into the tagged structure below (a shape change, not a rename).
- **Tagged `calculation_method` (`CONFIRMED`, discriminated on `chosen.kind`)** — `{ method_version: int, chosen: method_variant, considered: [method_variant] }`; `method_variant` is a `oneOf` `kind`-discriminated union: `rolling_aggregate {aggregation, window, filter?}` · `point_snapshot {field, filter?}` · `ratio {numerator, denominator, window?}` · `distribution_divergence {measure, window, baseline_window}`; `filter = {concept?, predicate?}`. **SP-3 switches on `chosen.kind` deterministically.**
- **`ASSUMPTION_LEDGER` (`schema_version:1`)** — extends SP-0's registered schema: top-level array is **`assumptions`** (the name SP-0's `ASSUMPTION_LEDGER_JSON_SCHEMA` requires, `documents/draft.py:47` — **not** `entries`); each item `required: [field, value, rationale]` (SP-0), + SP-2 semantic extras `{source ∈ {default,catalog,llm}, ambiguity, confidence, auto_resolved_at}`. Item value field is **`value`** (not `chosen_value`). Surfaced in full at Gate #1; `governance-retained`.
- **`confirmation` record (persisted at Gate #1)** = `{confirmed_by, confirmed_at, selected_candidate, rejected_candidates, human_edits[], ambiguity_notes}` (+ confirmer identity/role-claims/`source_of_authority`); rejected sibling `doc_id`s live **only here** (documents are write-once — no per-doc reject event).
- **Upcasters:** each schema is versioned; every bump ships a **total, chained, pure reader-upcaster** registered in SP-0's document registry (read-time projection, never a rewrite — DAG history + content-hashes preserved).

### `BankingDomainCatalog` reader + deterministic classification (§4.5, §5.4)
- **Reader:** read-only loader of the SP-0-governed `banking-domain-catalog` seed → `allowed_domains`/`allowed_use_cases`, `out_of_scope_examples`, `blocked_data_classes`, `sensitive_proxy_hints`, `jurisdiction_scope`/`use_case_scope`, `version`/`owner`/`effective_date`/`provenance`. SP-2 **reads only**; never writes; never grounding.
- **`classify_intent(intent, *, product=None, region=None, catalog) -> IntakeClassification`** — deterministic (not the LLM's call). Outcomes (`IntakeOutcome`): `OUT_OF_SCOPE` (terminal/park; reason + `version`), `PROHIBITED_DATA_CLASS` (terminal block; matched class + `version`; re-run authoritatively at §8.4), `SENSITIVE_PROXY_CLARIFY` (non-terminal → clarification/compliance review), `AMBIGUOUS_CLARIFY` (non-terminal → clarification), `NEEDS_USE_CASE_ONBOARDING` (in-scope, unknown use-case → park), `CLEAR` (pass). **Completeness (total, fail-closed):** (a) precedence most-restrictive-wins `PROHIBITED_DATA_CLASS > OUT_OF_SCOPE > sensitive-proxy > ambiguous` — exactly one outcome; (b) catalog unavailable/unversioned → fail closed (park); (c) `version` stamped on **every** outcome incl. `CLEAR`; (d) `version` recorded at intake, re-evaluated at confirmation for drift (flip → re-clarify); (e) missing product/region needed by scope → `AMBIGUOUS_CLARIFY`.

### SP-2 lifecycle guards (registered in SP-0's predicate registry; evaluated on frozen docs before append)
`open_fields_empty`, `minimum_contract_validated`, `not_prohibited_intent`, `calculation_method_chosen` (satisfied in hypothesis mode by a document `PRIMARY_SELECTED` promotion), `actor_is_request_owner`, `confirmer_is_requester_human` (= `actor_is_request_owner ∧ actor_kind=="human"`), `delegation_allowed=False` on SP-2-opened tasks.

### Interfaces exposed to SP-3+ (§13)
- **Commands:** `submit_intent`, `answer_clarification`, `select_candidate_doc`, `open_gate1_task`, `confirm_contract`, `request_edit`, `reject_intent` (+ requester `withdraw` reuse) — per the table above.
- **Read model:** `get_contract(conn, run_id) -> {stage, status, draft|confirmed body, assumption_ledger, field_scores, open_questions}` — service-internal, for SP-3 to fetch the Confirmed Contract.
- **The Confirmed Feature Contract document** (`CONFIRMED_CONTRACT` stage) — the governed hand-off artifact; `requires_independent_validation` rides it to Gate #2 (SP-5).
- **Authoritative content-schemas** (`DRAFT_CONTRACT`/`ASSUMPTION_LEDGER`/`CONFIRMED_CONTRACT`) — registered, versioned, closed enums, `UNKNOWN`, upcasters, tagged `calculation_method`.
- **`CandidateGenerator` protocol** (the seam SP-12 binds its real engine to), **`IntentRedactor` protocol + default impl**, and **`LLMClient` protocol + the auditable-LLM envelope** (reusable by SP-3/SP-6/SP-8/SP-12).

---

## Phases

Implement in order. **P1 unblocks everything.** Each phase file holds bite-sized TDD tasks with exact code; this overview holds the shared contract.

**P1 — SP-0 additive extensions** ([sp2-01](2026-07-01-sp2-01-sp0-extensions.md)) — *sequenced first.*
Register the twelve SP-2 event types (`events.py` + `register_sp2_event_types`); ship the `0508_use_case_onboarding_gates.sql` migration widening the `human_tasks` gate CHECK with `USE_CASE_ONBOARDING` and registering the `NEEDS_USE_CASE_ONBOARDING` park hold-state; add the `reject_intent` service authz row + the SP-2 command-capability rows in `seed_sp2_authz`; create the `llm_call` record-store table (write-once, classified sensitive) + its projection/checkpoint init; ensure `register_primary_selected` is wired for candidate promotion. Additive, backward-compatible, idempotent. *Depends on: SP-0.*

**P2 — Feature Contract data model + `BankingDomainCatalog` reader** ([sp2-02](2026-07-01-sp2-02-feature-contract.md)).
`contract.py`: the Draft/Confirmed/Ledger JSON-Schemas with closed enum vocabularies, the `UNKNOWN` sentinel (reusing SP-0's), the tagged `calculation_method` shape, the `assumptions` ledger array, `validate_semantics`, the deterministic Draft→Confirmed `assemble_confirmed` (renames + method reshape), and total chained reader-upcasters; `register_contract_schemas`. `banking_catalog.py`: the read-only `BankingDomainCatalog` reference loader + the deterministic `classify_intent` classifier with the complete (total, fail-closed) contract. *Depends on: P1 (schema/stage registration), SP-0 `documents/draft.py`.*

**P3 — Auditable-LLM envelope** ([sp2-03](2026-07-01-sp2-03-llm-envelope.md)).
`llm.py`: the `LLMClient` interface, `LLMRequest`/`LLMResult`, `FakeLLM` (scriptable to invalid/refusal/ambiguous), the structured-output + bounded-repair/retry/fail-closed taxonomy, `call_llm` (the event-sourced wrapper writing the `llm_call` record + `LLM_CALL_RECORDED`), and the full-identity idempotency key. `redaction.py`: the `IntentRedactor` seam + `DefaultIntentRedactor` + the `assert_llm_safe` egress guard. `llm_claude.py`: the config-gated real Claude adapter (lazy import, never in CI). *Depends on: P1 (`llm_call` store, event types), P2 (output-schema registration).*

**P4 — Layer-1 intake** ([sp2-04](2026-07-01-sp2-04-intake.md)).
The `submit_intent` command: `create_request`+`create_run` (DRAFT) → classify raw intent → banking-boundary classification (`OUT_OF_SCOPE`/`PROHIBITED_DATA_CLASS`→`reject_intent`, sensitive-proxy/ambiguous→clarify, unknown-use-case→`NEEDS_USE_CASE_ONBOARDING` park + `USE_CASE_ONBOARDING_REQUESTED`) → redact → `call_llm(structure_intent)` → frozen Draft Contract (`NEEDS_CLARIFICATION`) + Assumption Ledger → `DRAFT_CONTRACT_PRODUCED`. Enforce the no-silent-assumption rule (every non-verbatim field → an open question or a ledger entry). *Depends on: P2, P3.*

**P5 — Layer-2 clarification** ([sp2-05](2026-07-01-sp2-05-clarification.md)).
`scoring.py` (LLM self-report + deterministic catalog-cardinality check, cautious-max combine), `doubt_router.py` (auto-resolve iff `ambiguity ≤ 0.30 ∧ confidence ≥ 0.70 ∧ safe source ∧ not policy-sensitive ∧ not calc-method`; config-gated), `critique.py` (`CONTRACT_REVIEW` challenger feeding the Doubt Router), the Human Clarification tasks (`answer_clarification` wrapper with the request-owner guard + `delegation_allowed=False`), the bounded Refinement Loop (renormalize → rescore → re-route; exhausted → auto-park), and `mcv.py` Minimum Contract Validation (6 checks → `MINIMUM_CONTRACT_VALIDATED`). *Depends on: P4.*

**P6 — Hypothesis flow (`CandidateGenerator` seam + stub)** ([sp2-06](2026-07-01-sp2-06-hypothesis-candidates.md)).
`candidates.py`: the `CandidateGenerator` Protocol, `Candidate` schema, and the deliberately-dumb single-call `StubCandidateGenerator`; each candidate is written as a candidate-role staged document under the run's Draft stage; the `select_candidate_doc` handler performs the document `PRIMARY_SELECTED` promotion (write-once losers untouched). No predictive scoring, no learning loop, no gate bypass; the seam is generator-agnostic (SP-12 boundary). *Depends on: P3, P4, SP-0 `documents/primary.py`.*

**P7 — Human Gate #1** ([sp2-07](2026-07-01-sp2-07-gate1.md)).
`open_gate1_task` (only after MCV; dedicated `CLARIFICATION`-gate confirm task, `delegation_allowed=False`, cancels pending clarification tasks), `confirm_contract` (all guardrails: `confirmer_is_requester_human`, the §8.4 prohibited-intent block + version-drift re-check, `requires_independent_validation` risk-flag set, hypothesis-mode candidate promotion, writes `CONTRACT_CONFIRMED` + frozen Confirmed doc + the full confirmation record, DRAFT → CONFIRMED_CONTRACT), `request_edit` (REVISED Draft supersede + re-run MCV + re-open gate), and task-version OCC on every confirm/edit/reject. *Depends on: P5, P6.*

**P8 — Rejection/withdrawal + lifecycle state machine + SP-3 interfaces** ([sp2-08](2026-07-01-sp2-08-lifecycle-interfaces.md)).
Wire the `reject_intent` platform/service terminal outcome and the requester-`withdraw` reuse; register the SP-2 lifecycle guards in SP-0's predicate registry and declare the DRAFT → CONFIRMED_CONTRACT transition (guards evaluated before append); pin the exact SP-0 run-state representation; build the `get_contract` read model and finalize the §13 interfaces SP-3 binds to. *Depends on: P7.*

**P9 — Bootstrap + E2E** ([sp2-09](2026-07-01-sp2-09-bootstrap-e2e.md)).
`bootstrap.py`: `register_sp2(handler_registry)` (event schemas + contract schemas + commands + `PRIMARY_SELECTED`) + `seed_sp2_authz(conn)`; the two end-to-end running examples (definition `declined_card_auth_count_90d`; hypothesis abrupt-category-shift → credit risk) under **real authz + SoD** with `FakeLLM`, asserting exact Draft/Confirmed contracts, the request-owner guard denials, the auditable-LLM records, and the no-PII boundary. *Depends on: P1–P8.*

---

## Decisions / deviations register (binding — carried from spec §16)

| # | Point | What SP-2 did / where a call was made |
|---|---|---|
| 1 | Scope shape | Definition mode end-to-end + all shared Layer-1/2 machinery; hypothesis is a real flow with a single-call **stub** generator; the SP-12 boundary holds (seam only). |
| 2 | No new aggregate | Rides SP-0's existing `run` aggregate + DRAFT/CONFIRMED_CONTRACT states + `CLARIFICATION` gate + the document `PRIMARY_SELECTED` candidate-promotion primitive (candidate selection is document-level, **not** request-level `select_candidate`) — **no new aggregate, no event-store aggregate-CHECK migration** (unlike SP-1). Additive only: event-types, document-schemas, and one backward-compatible gate/park migration (`0508`, mirroring SP-1's `0505`). |
| 3 | Ambiguity/confidence scale + combine | Fixed a **0.0–1.0** scale for both, sourced from LLM self-report **+** a deterministic catalog-cardinality check, platform taking the **more cautious** value on disagreement (LLM can never lower a deterministic doubt). |
| 4 | Doubt Router thresholds | `auto-resolve iff ambiguity ≤ 0.30 ∧ confidence ≥ 0.70 ∧ safe source ∧ not policy-sensitive ∧ not calc-method`; config-gated, biased toward asking. |
| 5 | Bounded repair budget + refusal disposition | Default **N=2** structured-output repair attempts for *malformed structure*, config-gated, then fail into clarification. A `stop_reason=="refusal"` is a **policy decline → fail into clarification directly** (not repair). |
| 6 | Refinement-loop bound | Loop bounded by SP-0's durable-runtime hard loop limit; on exhaustion **auto-park** the run for human follow-up. |
| 7 | LLM-call record store | Modelled as an **SP-2-owned immutable append-only `llm_call` store** (mirroring SP-1's evidence store) referenced by `llm_call_ref`, + an `LLM_CALL_RECORDED` event — SP-0's stage/artifact enum has no LLM-call type (Decision D9). |
| 8 | Banking-scope / `BankingDomainCatalog` dependency | **RATIFIED (user-approved).** Accepted as **SP-0-governed, read-only intake-classification reference data** — never grounding/execution — so it is *not* an SP-2 build dependency and does not violate "SP-0 only". Deterministic outcomes: `OUT_OF_SCOPE`, `PROHIBITED_DATA_CLASS` (both fail-closed, stamp reason/class + `version`); sensitive-proxy/ambiguous → clarify; new use-case → onboarding park. |
| 9 | No-PII enforcement construction + redactor ownership | Three points, clean ownership split — **SP-0 classifies** (`raw_input_classification`; its `assert_no_inline_pii` is a classification guard, not a redactor), **SP-2 redacts** via the `IntentRedactor` seam (fails closed on un-redactable/`unscanned`), **SP-2 guards egress** (pre-send hard-fail → security-audit). `input_redaction` recorded for audit. |
| 10 | Prohibited-intent mechanism | **Deterministic** screen over `blocked_data_classes`: prohibited class → `PROHIBITED_DATA_CLASS` block (matched class + `version`) → edit-and-loop / requester `withdraw` / platform `reject_intent`; a `sensitive_proxy_hints` match is the **distinct** routing → clarification/compliance review, **not** an auto-block; never an LLM judgement, never a compliance approval. |
| 11 | Gate #1 is not four-eyes | Author confirms own intent (audited intent lock); `requires_independent_validation` is a **flag only**, no second signer; independent validation is Gate #2 / SP-5. Does **not** invoke SP-0's `FINAL_APPROVAL` SoD. |
| 12 | Real adapter details | Anthropic SDK, model `claude-opus-4-8`, adaptive thinking, structured outputs via `output_config.format`; `refusal` → fail into clarification, `max_tokens`/timeout/429/5xx → bounded retry, non-retryable 4xx → fail-closed, no-PHI-in-schema; **no fallback to `FakeLLM`**. |
| 13 | Rejection / withdrawal authority | SP-2's deterministic intake rejections (`OUT_OF_SCOPE`/`PROHIBITED_DATA_CLASS`) are **platform/service-issued** via SP-2's own `reject_intent` (→ `RUN_REJECTED`) under one additive service authz row — **not** SP-0's validator-only `reject` (`authz/policy.py:42`). Requester abandonment reuses SP-0 `withdraw` (`authz/policy.py:41`, → `RUN_WITHDRAWN`). The added row changes no existing SP-0 row; SoD holds. |
| 14 | `BankingDomainCatalog` classifier — completeness | (a) precedence most-restrictive-wins → exactly one outcome; (b) unavailable/unversioned → fail-closed park; (c) `version` stamped on **every** outcome incl. CLEAR; (d) version drift re-evaluated at confirmation (flip → re-clarify); (e) missing product/region → ambiguity → clarify. All deterministic, all fail-closed. |
| 15 | LLM-call retention — stored-redacted, not hash-only | The record stores the **redacted (LLM-safe) input itself** (`redacted_input` + `redaction_version`), not a bare `input_hash` — hash-only cannot be replayed/reviewed (defeats MRM/adverse-action reproducibility). Raw intent stays in SP-0's encrypted `raw_input_ref`; the store is sensitive/governance-retained/read-controlled. |
| 16 | LLM idempotency key + provider-failure taxonomy + no-PHI-in-schema | (a) Idempotency key widened to the **full call identity** (`run_id, task, input_hash, provider, model, prompt_id, prompt_version, output_schema_id, output_schema_version, redaction_version, generation_settings`); (b) full fail-closed provider-failure taxonomy; (c) no PHI/PII in the output-schema's property names / enums / descriptions (server-compiled, cross-call-cached). |
| 17 | Authoritative Draft/Confirmed/Ledger content-schemas | Registered JSON Schemas with required fields, stable field paths, closed enum vocabularies, the `UNKNOWN` sentinel (must be in `open_fields`), total chained reader-upcasters, the versioned/tagged `calculation_method` (SP-3 switches on `chosen.kind`), the `assumptions` ledger array (item `value`), and the SP-0 envelope fields carried into the Confirmed stage. |

---

## Appendix — the two running examples (end-to-end, from §Appendix B)

**Definition — `declined_card_auth_count_90d`.** `submit_intent("90-day rolling count of declined card authorizations per customer", mode=definition)` → Draft (entity `customer`; grain `customer × as_of_date` [ledger: default]; method `rolling_count` [ledger]; window `90d` [ledger]; filter `declined authorization` → **open, must-ask**, amb 0.80). One `CLARIFICATION` task to the requester → `auth_result = 'D'`. Re-normalize → all fields resolved → MCV passes → prohibited-intent screen clear → Gate #1: requester confirms the faithful translation → **Confirmed Contract**, `requires_independent_validation=false` → SP-3.

**Hypothesis — abrupt spending-category shift → credit risk.** `submit_intent("customers who abruptly shift spending category are higher credit risk", mode=hypothesis)` → Draft with `target="higher credit risk"` (policy-sensitive, must-ask) + calc-method must-ask. `StubCandidateGenerator` makes **one** LLM call → 3 candidate documents (distinct-MCC delta; top-category-share drift; distribution divergence), each with a rationale + cheap `signals` (no IV/WoE). Risk-flag screen fires → `requires_independent_validation=true`. Prohibited-intent screen: target pinned to a permitted, non-blocked label (a `sensitive_proxy_hints` match → clarification/compliance review; a `blocked_data_classes` protected attribute would **block** as `PROHIBITED_DATA_CLASS`). At Gate #1 the requester picks one candidate (document `PRIMARY_SELECTED`; losers recorded in the confirmation record only), confirms the pinned target → **Confirmed Contract** (`intake_mode=hypothesis`, `selected_candidate`/`rejected_candidates` recorded, `requires_independent_validation=true`) → SP-3 (and the flag drives Gate #2 at SP-5).
