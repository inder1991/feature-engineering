# Cross-Catalog Serving — "First Served Card" Implementation Plan (REV 7, executable)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Rev 7 is SELF-CONTAINED and TASK-EXPANDED — it replaces Revs 1-6; nothing binding lives in git history. Seven review rounds (2026-08-24) are absorbed; every falsifiable finding was verified at origin/main `463498ed` before adoption. Parent spec: `docs/superpowers/plans/2026-08-23-cross-catalog-program-rev5.md` §Stage-2, amended by Task P1. Stage-1 rigor: T0 first, fresh implementer per task, review-gated, ledgered rulings, final whole-branch review. NO merge/push/deploy/flag-flip without explicit user go.

**Goal:** A hypothesis surfaces cross-catalog feature cards from LOGICAL plans over AI-proposed-or-better links; every action state is the canonical decision service's verdict through typed per-action fact loaders; generation carries the exact user-confirmed plan; production renders "unavailable — governance not released".

## Owner's serving policy (binding)
Card/rank/author: allowed on AI-proposed links. Preview: exact join encodable → full; else provisional with runtime gates. Sandbox execution: BLOCKED this scope (`EXECUTION_SOURCE_COMPATIBILITY_UNPROVEN`) — CI fixture validation only. Production: unavailable until governance releases. Human link review changes display provenance only.

## Rulings (ledger-bound)

- **R1 — one authority, TYPED fact loaders.** Six-action service (`materialize/action_decision.py`) sole authority from AUTHOR_FORMULA on; legacy ladder keeps save_idea+create_contract; other legacy rungs become adapters/retire (disagreement inventory first; canonical wins — proven row `RECIPE_REVIEW_NOT_CURRENT`). Facts come from ONE loader PER ACTION with a TYPED subject: `facts_for_author_formula(conn, authoring_subject_revision)`, `facts_for_generate_preview(conn, build_set_revision_id)`, `facts_for_execute_sandbox(conn, sealed_artifact_id)` — consumed by ask, decide, worker recheck, and projection. **Before the action's resource exists, a card shows "potentially available after selection" — a projection, never a durable decision** (a recommendation card has no build set; pretending otherwise fakes a verdict).
- **R2 — execution context never touches logical identity.** Context lives only in `PhysicalExecutionPlanV1`; formula reuse keys on the logical digest; new environment → new physical revision, never a re-bought formula.
- **R3 — physical adoption = append-only user-confirmed chain WITH a workflow.** Table + API + UI (Tasks B2b/C4): `GET /selections/{id}/candidate-execution-plans`, `POST /selections/{id}/physical-plan-adoptions` with `expected_predecessor` CAS; confirmation UI shows environment, join keys, predicates, cardinality. DDL proves: adoption's physical plan shares the selection's logical digest (FK through the binding); at most one successor per predecessor (UNIQUE on supersedes); no forked chains (CAS refusal test under concurrency); repeat confirmation idempotent (content_hash); build-set member pins exact adoption + render profile. Until the FIRST adoption exists, preview is simply not yet requestable for that selection — the rule gates preview, it cannot deadlock cards/formulas.
- **R4 — preview generates; sandbox never executes this scope.** Generated code runs only in isolated CI fixture validation. Cards distinguish *Preview rendered* / *Fixture validated* (artifact-bound only) / *Sandbox executable* (never claimable here).
- **R5 — synchronous identity, asynchronous telemetry (round-6 corrected).** The serving transaction SYNCHRONOUSLY persists `LogicalFeaturePlanV2`, the considered-option binding, and activation facts — identity lineage is never fail-soft and never awaits a worker (B2's commit-time totality depends on it). The outbox carries TELEMETRY ONLY. Migration 1121 is applied and immutable (its inputs-payload + replan design stands for legacy items): a NEW versioned result-outbox table carries serialized live RESULTS; the worker persists them verbatim and never replans live work; the legacy worker path keeps draining legacy input items; shadow planning is a separate experiment lane excluded from live denominators. Demand: once per occurrence via `demand_identity_hash`.
- **R6 — reuse platform codes.** Cardinality reason = existing `DIRECTIONAL_CARDINALITY_UNPROVEN` (verified `W,W,W,W,B,B`). `CARDINALITY_EVIDENCE_REQUIRED` = demand-queue category only. `REALIZATION_ATTACHMENT_DEFECT` = ops alert.
- **R7 — promotable class = "LLM deterministic intent awaiting formula", exactly** (the demotion at `feature_planning_contracts.py:248-252` is the seam). Promotion re-projects original intent bytes + logical digest + operation class + bound operands + target/leakage re-eval → immutable `authoring_subject_revision`; `request_draft_for_candidate` consumes the promoted subject; conceptual patterns stay blocked.
- **R8 (narrowed — round-6 P0-3) — supersede ONLY the plan-envelope overlap.** From the remediation plan (NO-GO), this plan absorbs: the fact-loader requirement (as R1's typed loaders) and the plan-envelope columns of its 1122/1124 designs. This plan PRESERVES and extends-with-a-logical-plan-reference: target-reading agreement across jobs/selections/build sets; frozen formula strategy + review-event identity; expectation generation; provider contract + spend identity; method override identity; the worker prohibition on recomputing strategy. Task P1 records the split in both documents.
- **R9 — parameter identity is semantic, not conversational (round-6 P0-7, verified: the chooser's content address includes the hypothesis).** The logical digest carries {parameter_spec_revision, ordered (name, value) pairs} for ALL parameters — never the chooser revision (two hypotheses choosing 90d must share one feature identity, one formula). Chooser revision → provenance/evidence pins. The TWO existing chooser implementations (`contract/param_choice.py` serving lane; `overlay/upload/param_choice.py` shadow) consolidate under ONE owner module (B7) — promotion never adds a third.

## Identity payloads (the executable spec)

```
LogicalFeaturePlanV2: canonical_definition_id, definition_origin, operation_class,
  ordered operand_bindings (operand_ref, role, concept, catalog-qualified column),
  output_grain_key_refs (ORDERED tuple), parameters {spec_revision, ordered (name,value) pairs},
  logical_bridge_path (ordered: link semantic revision, from/to entity, FULL ordered endpoint
  tuples, traversal direction), formula_policies, planning_request_hash → logical_digest.
PhysicalExecutionPlanV1: logical_digest, execution_context_revision_id (bound to existing
  environment_id/ExecutionTier), source_binding_revisions, per-segment {realization_revision,
  exact ordered column pairs, predicates, directional cardinality, realization content+dependency
  hashes}, join_validation_policy_revision_id → physical_digest.
RenderProfileV1: engine, compiler_version, renderer_version → render_digest.
Composed generation digest = sha256(logical|physical|render).
JoinValidationPolicyRevisionV1 (CLOSED semantics, round-6 P0-8): null_key_behavior ∈
  {exclude_row, refuse}, unmatched_row_behavior ∈ {preserve_left_null, exclude, refuse},
  coverage {numerator: matched left rows, denominator: eligible left rows post-predicates,
  minimum}, orientation (left=source side, explicit), max_matches_per_left_row + interpretation
  (>1 permitted ONLY with declared aggregation-safety note), snapshot/cutoff ref, declared_by,
  policy_revision_id, content_hash.
JoinValidationObservationV1 (immutable): sealed_artifact_id, logical/physical/render digests,
  policy_revision_id, input_snapshot_id, measured counts (left rows, matched, max matches,
  coverage), runtime+compiler versions, PASS|FAIL, recorded_at.  # evidence NEVER transfers
  between plans — the digests bind it.
```

## Identity & staleness law (binding; every task tests the rows it touches)
Proposed→confirmed: no rekey. Strength/ranking: no rekey. Candidate withdrawn / realization revoked: refuse. New realization supersedes: refuse old pin; R3 adoption is the only path forward. Display-evidence text: no rekey. Join column/predicate/cardinality: NEW plan identity. Environment/tier: new physical revision only. Same parameters via different hypotheses/choosers: SAME logical identity (R9).

## Disposition matrix — all six actions explicit (registered via three-part commits; CI exhaustiveness green)

| Reason | AUTHOR_FORMULA | GENERATE_PREVIEW | EXECUTE_SANDBOX | PUBLISH_SANDBOX | MATERIALIZE_PROD | PUBLISH_PROD |
|---|---|---|---|---|---|---|
| DIRECTIONAL_REALIZATION_MISSING | Warn | Block | Block | Block | Block | Block |
| DIRECTIONAL_MAPPING_INCOMPLETE | Warn | Block | Block | Block | Block | Block |
| EXECUTION_CONTEXT_MISSING | Warn | Block | Block | Block | Block | Block |
| EXECUTION_SOURCE_COMPATIBILITY_UNPROVEN | Warn | Warn | Block | Block | Block | Block |
| OPERAND_ROLE_UNRESOLVED | Block | Block | Block | Block | Block | Block |
| JOIN_NULL/COVERAGE/MAX_MATCH_POLICY_MISSING | Warn | Block | Block | Block | Block | Block |
| ALLOCATION_POLICY_REQUIRED (final-grain) | Block | Block | Block | Block | Block | Block |

M:N source-grain intermediate (round-6): its own FIRST-CLASS feature identity — a distinct `LogicalFeaturePlanV2` at the SOURCE grain (its own card, formula, bindings) — never an informal exception to the final-grain block.

## §V verified facts (seven rounds, cumulative — T0 re-verifies ALL at then-current origin/main)
- V1. `active_bridges` serves proposed+confirmed alike; `ActiveBridgeV1` lacks candidate/evidence revisions; `members[0]` collapse loses composite keys (`bridge_projection.py:42-56,165,168`).
- V2. G3 minted during compilation; lens consumes compiled results (`governed_lens.py:580,648`) — unrealized links yield NO card today. G2 (MEASURE-defaulting, `need_metadata.py:84,108`; 82-operand worklist `governed_lens.py:55-63`) unmasks when cardinality attaches (`declarations.py:716`).
- V3. `attach_executable_bridge_realizations` (`assembly.py:404`); executable reader excludes M:N (`bridge_store.py:125`), hardcodes PRODUCTION (:900); `revalidate_bridge_realization` (:776, tier defaults PRODUCTION :782, refuses mismatch :795-800) → SANDBOX-scoped realization rows required.
- V4. Serving unreachable: `routes/contract.py:776`; `gate1.py:1161` vs `elif is_live:` :1308.
- V5. `selection_formula_binding.py:32` pins selection+formula+`planning_request_hash`+`binding_plan_hash` (:41-42); `restore_formula_v3.py:91` no plan; `ResolvedFeatureInputV2` forbids optional plan field (`admission_v2.py:94-100`); join re-resolution `materialize/joins.py:920`; `feature_selection_revision` (1072:83) append-only, no supersession → R3; drafts precede selection.
- V6. Renderer `{schema}.{table}` keys (`render/project.py:348-350`) vs `catalog::schema.table` (`wiring.py:199`); spine/gate manifests/source-binding validation unqualified.
- V7. 1120 verdict CHECK closed; 1121 applied+immutable (inputs+replan design) → R5's NEW result outbox.
- V8. Two authorities disagree live (`RECIPE_REVIEW_NOT_CURRENT`: `activation_policy.py:152` blocks vs `action_dispositions.py:136` warns); `DIRECTIONAL_CARDINALITY_UNPROVEN` exists :128; caller-assembled facts diverge (`code_generation_jobs.py:272` with blockers vs `generation_lane.py:282` bare names; resource identity = build_set_revision_id).
- V9. **Grain law (round-6):** `posted_debit_amount` anchors `entity("account","account_id","transaction")` (`transaction_foundation.py:67-70`) — ACCOUNT grain; the lens's B10 rule refuses UOA mismatch (`recipe_planning_lens.py:259`); it is the ONLY reviewed V2 expectation. NO count recipe carries a structural row_selection (registry probe: 0; "debit" is prose in `posted_debit_transaction_count`). The demotion seam `feature_planning_contracts.py:248-252`. Blueprint: count→COUNT_ROWS consumes no operand (`recipe_formula_blueprint_derivation.py:99`) — dimension-bearing recipes compute TOTALS.
- V10. `FEATUREGEN_MATERIALIZE_INVENTORY` commented (`20-backend.yaml:197`); activation flag + signed gate (`live_activation.py:56,242`); `PLAN_CONTRACT_VERSION` in vector (:62) → bump; chooser content address includes hypothesis+prompt version (`param_choice.py:96`) → R9; two chooser modules exist (`contract/param_choice.py`, `overlay/upload/param_choice.py`).
- V11. Remediation plan: NO-GO banner; Task 5 (1122 lineage) + Task 8 (1124 strategy/method identity) carry NON-overlapping safety work → R8 narrowed. Migration registry: its 1121-1129 block (1121 dead — ours live); 1117 owner unknown; OUR block 1130+ after T0 reconciliation.

---

# TASKS (numbered; each is one SDD dispatch with its own TDD cycle, tests-first, and commit)

**T0 — verification + registries.** Re-verify V1-V11 + all seven rounds' citations at then-current origin/main; R1 adapter-surface inventory (all legacy-rung callers); canonical `ActionV1` members + ask/decide/recheck entry points; candidate-revision identity material; 1117 owner; migration-registry reconciliation (our 1130+ block) + R8 split recorded; pick J-fixture catalog names. Ledger deltas; amend tasks before dispatch. *No code.*

**P1 — parent amendment + supersession record.** Amend the parent: Stage-2 gate split (implementation now / ACTIVATION behind signed thresholds + wave-1 + latency + inventory health + signed artifact); S2-P4 = composed three-layer carrier; S2-P6 split (execution-source blocks EXECUTE_SANDBOX, not GENERATE_PREVIEW); R8's narrowed split recorded in BOTH plans. *Docs; commit per file.*

## Phase A — discovery tells the truth
**A1 — capability + fact-loader consolidation (R1).** Files: `materialize/action_facts.py` (new: the three typed loaders + `ActionFactsV1`), `materialize/action_decision.py` (consume), `overlay/upload/activation_policy.py` (adapters for the three legacy rungs), `api/routes/code_generation_jobs.py` + `materialize/generation_lane.py` (converge on loaders), `overlay/upload/contract/capability.py` (new: ladder + projection incl. "potentially available after selection"), three-part registrations for the matrix. Tests: disagreement-inventory regression rows; property test — no projected action contradicts ask/decide fed by the loaders; pre-resource card copy pinned. Commit: `feat(decisions): one authority, typed per-action facts`.
**A2 — projection extension.** Files: `overlay/upload/bridge_projection.py` (+ full ordered endpoint tuples, link semantic revision, evidence split; drop `members[0]`), consumers in `planner/assembly.py`. Tests: composite-key round-trip; display-evidence change does NOT rekey (staleness rows); no new candidate-ledger reader (grep-pin). Commit: `feat(bridges): the projection carries what plans need`.
**A3 — execution context.** Files: new `overlay/upload/execution_context.py` + migration (1130-block): immutable revision bound to existing `environment_id`/`ExecutionTier`; absence = `EXECUTION_CONTEXT_MISSING`. Tests: revision immutability; logical identity provably unchanged across contexts (R2 row). Commit: `feat(context): server-owned execution context revisions`.
**A4 — realization snapshot.** Files: `overlay/upload/bridge_store.py` (+`BridgeRealizationSnapshotV1`, constant-query batch, executable+provisional rows), `planner` consumers; extends `CompileBudget` cap/deadline; truncation cause persisted+disclosed. Tests: query-count pin; truncation determinism. Commit: `feat(planner): one frozen realization snapshot per request`.
**A5 — logical resolution without physical evidence (DECLARED identity change).** Files: `planner/` (logical resolution with deferred physical cardinality → real `LogicalFeaturePlanV2` for card/formula rungs; PLANNER_VERSION bump; Stage-1 literal pins regenerated deliberately), `formula_draft_worker` grain_refs source. Tests: unrealized proposed link → logical plan resolves; G3 physical refusal preserved for physical rungs; pins regenerated in-commit. Commit: `feat(planner): logical plans resolve; physical evidence upgrades`.
**A6 — G2 serving gate + journey fixture reality.** Files: `need_metadata.py`/lens (OPERAND_ROLE_UNRESOLVED for every potentially-served option outside the resolved set), fixture catalogs for journeys: **account-grain** (V9): "ops" transactions catalog + "core" accounts catalog, AI-proposed `account_number ↔ acct_id` link; hypothesis at ACCOUNT UOA ("accounts whose posted debit outflows spike…"). Also: `posted_debit_transaction_count` gains its STRUCTURAL debit row_selection (engineering; the operator review entry is an E-rail lever). Tests: G2 gate excludes an unresolved candidate from FORMULA_AUTHORABLE; the fixture plans clean at account grain; UOA mismatch still refuses at customer UOA (B10 pin). Commit: `feat(serving-gate): operand-role resolution gates eligibility; honest journey fixtures`.
**A7 — demand vocabulary + one writer.** Files: migration (1130-block) demand categories (incl. `CARDINALITY_EVIDENCE_REQUIRED` as category), `governed_observation_store` extension, satisfaction projection; R5 dedupe law. Tests: once-per-occurrence; category routing; history immutable. Commit: `feat(demand): the new vocabulary, one writer, satisfaction projection`.

## Phase B — the pinned plan
**B1 — three-layer identity model.** Files: `planner/logical_plan_v2.py`, `planner/physical_plan_v1.py`, `planner/render_profile.py` + migrations (1130-block, relational + FKs); canonicalization + digests. Tests: round-trips; same-read-set/different-shape → different digests; R9 parameter-identity pin (two hypotheses, same values → one digest); ordered grain tuple survives serialization. Commit: `feat(identity): logical, physical, render — composed`.
**B2 — total binding chain (DDL).** Files: migrations + stores: `considered_option_plan_binding`, `formula_draft_plan_binding` (NOT the existing 1104 name), `selection_formula_plan_binding`, `selection_physical_plan_adoption_revision` (R3 columns + constraints), build-set member → combined binding; parent-carried FK where insert order allows, deferred constraint trigger where not; digests check against existing `binding_plan_hash`/`planning_request_hash`. Tests: the five orphan-refusals; adoption-chain CAS under concurrency; idempotent re-confirmation; legacy rows refused for cross-catalog generation. Commit: `feat(bindings): disagreement unrepresentable`.
**B2b — the adoption workflow (R3).** Files: `api/routes/selections.py` (GET candidate-execution-plans; POST physical-plan-adoptions with expected_predecessor CAS), store functions. Tests: CAS refusal; candidate list shows environment/join keys/predicates/cardinality; adoption unlocks preview requestability. Commit: `feat(adoption): the user confirms the exact join`.
**B3 — authoring consumes the pinned logical plan.** Files: `formula_draft_service`/worker (+logical digest into request content, provider audit, evidence pins, validation); `PinnedResolvedFeatureInputV3` wrapper through `restore_formula_v3` → `admission_v2` → `compile_ir_v2` (joins FROM pinned revisions; revalidation with pinned context; diagnostic-only fresh runs). Preserves R8's kept contracts (target lineage, frozen strategy, no-recompute) — extended with the logical-plan reference, never replaced. Tests: authoring-without-digest refused; legacy drafts refused for cross-catalog; Plan-A/Plan-B story refused at admission AND compile. Commit: `feat(pinning): the confirmed join is the generated join`.
**B4 — provisional lane.** Files: provisional assessment + reader (production reader untouched), provisional join IR (exact pairs+predicates), `compile_expression` wiring, renderer support, the PRE-AGGREGATION gate consuming `JoinValidationPolicyRevisionV1` (absent → matrix refusal), `JoinValidationObservationV1` writer. Tests: gate semantics per the closed policy enums; FAIL atomicity (no output, no partial aggregate, terminalized not retried, queue completed, dashboard names it, observation bound to digests+snapshot); evidence non-transfer (a pass under plan X refuses as evidence for plan Y). Commit: `feat(provisional): runtime-gated joins, artifact-bound evidence`.
**B5 — LLM promotion machinery (R7).** Files: promotion validator + API + `authoring_subject_revision` store (migration), decision-service integration, spend via existing cost-confirmation, provider audit identity; `request_draft_for_candidate` consumes promoted subjects. Tests: the deterministic-awaiting-formula class promotes end-to-end on FakeLLM; conceptual patterns still refuse; leakage re-eval pinned. Commit: `feat(promotion): deterministic intents earn formulas`.
**B6 — artifact-bound fixture evidence.** Files: migration + validation-runner write of the immutable {artifact, digests, fixture-set version, result} record; card wording rules ("Fixture validated" only from the record; else "Renderer tested on platform fixtures"). Tests: claim gating. Commit: `feat(evidence): fixture claims bind to artifacts`.
**B7 — ONE parameter chooser, promoted (R9).** Files: consolidate `contract/param_choice.py` + `overlay/upload/param_choice.py` under one owner; serving path: bounded menus, ALL ordered parameters, hypothesis-conditioned audited call (content-address replay) with deterministic fallback; `parameter_choice_revision` → provenance pins; values → the logical digest. Tests: "90-day hypothesis never gets a 30-day feature" (lookback vs horizon separated); fallback recorded; R9 identity pin. Commit: `feat(chooser): one owner, serving-grade, semantically neutral identity`.

## Phase C — rendering + serving (wired last)
**C1 — one dataset-key dialect.** `catalog::schema.table` through raw keys, dataset names, node inputs, catalog YAML, spine validation, gate manifests, source-binding validation; RENDERER_VERSION bump (in `RenderProfileV1`). Tests: two catalogs same `schema.table` distinct end-to-end; wrong-catalog spine REFUSES. Commit: `feat(render): catalog-qualified everywhere`.
**C2 — serving under one verdict + synchronous identity (R5).** Files: `gate1.py` branch restructure (engine + governed additive, anchor rule), `routes/contract.py` (activation object resolved once: flag state, signed approval identity, artifact hash+expiry, cohort approval, version vector, reasons — pinned into the option; draft/confirm revalidate same facts), synchronous persistence of logical plan + binding + activation facts IN the serving transaction, NEW result-outbox (migration) for telemetry, serving ceiling (cap/deadline/one snapshot read/disclosed truncation), first-serve ranking (hypothesis relevance, bridge tier, fan-out risk, semantic completeness, formula readiness, data availability), `GOVERNED_SERVING_POLICY_VERSION` + `PLAN_CONTRACT_VERSION` bump into `current_version_vector()` SAME commit. Tests: byte-identity inactive; anchor violation impossible; stale-approval; identity-persistence never fail-soft (poisoned outbox ≠ lost lineage); ceiling pins. Commit: `feat(serving): governed cards, one verdict, synchronous identity`.
**C3 — twin identity end to end.** All intermediate maps + persistence keyed option_id/governed_variant_id; canonical id stays in its column. Tests: engine+governed twin survives every stage. Commit: `fix(identity): twins never collide`.
**C4 — the card experience.** Four-section home (**Ready to generate / Formula available / Discoveries requiring setup / Not suitable**); proposed-link no-realization cards under "Discoveries requiring setup" with named blockers + remedies; provenance; provisional warning; the three execution-truth states; "potentially available after selection" pre-resource copy; production = governance-not-released; ALL states backend-driven; `api.ts` contract; adoption confirmation UI (B2b's modal); accessibility + responsive tests. Commit: `feat(frontend): the honest cross-catalog card`.

## Phase D — journeys (public APIs; CI fixture validation only; account-grain fixtures from A6)
**D1** no realization: card+formula; preview `DIRECTIONAL_REALIZATION_MISSING`; demand once. **D2** unknown cardinality (mapped link, no proof): provisional preview; runtime PASS + FAIL; reason `DIRECTIONAL_CARDINALITY_UNPROVEN`. **D3** exact N:1 (realization + confirmed adoption): full preview; IR asserts cross-catalog join step, two catalog-qualified inputs, bridge-gate node, ordered grain tuple, ONE composed digest card→selection→build→IR→render. **D4** known M:N: final-grain blocked `ALLOCATION_POLICY_REQUIRED`; SOURCE-grain intermediate = its own first-class plan/card; demand recorded. **D5** LLM-fallback value journey (`FORMULA_BLOCKED` recipe; spend authorization EXPLICIT; FakeLLM; method provenance) over the adversarial corpus WITH per-case expectations pinned in the task brief: VALUES for duplicates, reversals, debit/credit signs, late posting, post-cutoff, closed/dormant, joint-account M:N (source-grain), lookback-vs-horizon; NAMED REFUSALS for multi-currency-FX and reference-enrichment cases (out of scope — the refusal IS the assertion), missing bridge coverage. **D6** genuine LLM-origin: deterministic-awaiting-formula → B5 promotion → formula → bindings → build set → cross-catalog preview.

## Phase E — operator rail (every step explicit user go)
E1 migrations backend-first; telemetry + worker; `FEATUREGEN_MATERIALIZE_INVENTORY` + mappings + SANDBOX-scoped realization seeding; health checks. E2 satisfaction projection; report denominators exclude shadow. E3 operator levers: review entry for the fixed count recipe; AML expectation reviews. E4 targeted-cohort activation ONLY after P1's amended gate passes; SME thresholds + wave-1 gate BROAD.

# Not in scope (chartered; refused by name where reachable): M:N allocation policies; dimension/reference-enrichment joins; full S2-P6 beyond R4's minimum; public sandbox execution; federated execution; propose-bridge surface; promotion beyond R7's class; per-dimension features until parameterized row filters exist; customer-grain journeys over the account-anchored reviewed recipe (V9 — until a customer-grain recipe is reviewed or account→customer aggregation is chartered in).

# Execution
SDD, Stage-1 protocol; T0 first; ledger at `.superpowers/sdd/<plan-basename>/progress.md`; final whole-branch review (most capable model); NO merge/push/deploy/flag-flip without explicit user go.
