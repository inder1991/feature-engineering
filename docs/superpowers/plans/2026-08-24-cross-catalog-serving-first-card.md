# Cross-Catalog Serving — "First Served Card" Implementation Plan (REV 11, executable)

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development. Rev 11 is SELF-CONTAINED — it replaces Revs 1-10. ELEVEN review rounds (2026-08-24) absorbed; every falsifiable finding verified at origin/main `463498ed` before adoption (round 11: `record_realization_revision` exists with NO non-test producer from AI links; `DatasetTemporalPolicyRevisionV1` + `KnowledgeTimeBasisV2` are the existing temporal authorities — the latter names LATEST_AVAILABLE as leakage; `action_available` is "policy-available" while the run rail folds separate switches; `CompilationIdentityV2` already carries formula/IR hashes and generate_v2 hashes the actually-rendered files). **Round-11 gate: T0 and P1 are DISPATCHABLE NOW; Phase A/B implementation waits on this revision's P0 closures being review-confirmed.** Parent spec: `2026-08-23-cross-catalog-program-rev5.md` §Stage-2 amended by P1. Stage-1 rigor; NO merge/push/deploy/flag-flip without explicit user go.
>
> **TWO OWNER-RATIFIED PRODUCT RULINGS (adopted per the round-10 SME recommendation; the owner confirms or overrides at GO):** (1) **Duplicate transaction identity:** governed canonical transaction identity; the count REFUSES when uniqueness is violated unless an approved deduplication policy names the surviving record — never a silent dropDuplicates (COUNT_ROWS inflates, COUNT_DISTINCT silently collapses; both lie differently). (2) **The direct code-generation route uses the SAME coordinator service as the run lane** — one journey, one capability set; if the route is later retired it returns an explicit retired-route response, never a second journey with different capabilities.

**Goal:** From the hypothesis/run endpoint to a rendered cross-catalog preview, every step carries the identity the user confirmed; every action state is the canonical service's verdict through six typed fact loaders; production renders "unavailable — governance not released".

## Owner's serving policy (binding; round-11 preview rule tightened)
Card/rank/author: allowed on AI-proposed links (human review = display provenance only). Preview, three tiers: known-safe cardinality (1:1/N:1 proven) → FULL; exact ordered column mapping + UNKNOWN cardinality → PROVISIONAL with runtime guards; missing realization OR incomplete mapping → BLOCKED by name. Sandbox execution: BLOCKED this scope — generated code runs only in isolated CI fixture validation. Production: unavailable until governance releases.
**Round-11 P0-1 — the AI-link → provisional-realization production path (new Task B2a, before B2b):** `record_realization_revision` exists but NOTHING produces a realization from an AI-proposed link — today's journey dead-ends at `DIRECTIONAL_REALIZATION_MISSING` forever. B2a builds the server-owned proposal: AI-proposed link → ordered endpoint mappings (A2's full tuples) → frozen physical source bindings → SANDBOX-scoped directional realization with cardinality UNKNOWN → temporal + join policies attached (B3c/B2b surfaces) → a provisional physical plan → generated guards. NO human confirmation required (owner policy) — it stays provisional until deterministic evidence exists; the mapping-incomplete case refuses instead of guessing.

## Rulings (ledger-bound; R-numbers stable across revs)

- **R1 — one authority, SIX loaders that load FACTS only.** The six-action service is sole authority from AUTHOR_FORMULA on (legacy keeps save_idea+create_contract; other rungs adapter/retire; canonical wins — proven `RECIPE_REVIEW_NOT_CURRENT` row). Loaders — ONE immutable resource identity each (round-10: no slash-alternatives in an identity contract): AUTHOR_FORMULA → the 1103 subject key; GENERATE_PREVIEW → the build-set REVISION whose content identity includes the ordered combined member bindings + the generation configuration; EXECUTE_SANDBOX → sealed artifact; PUBLISH_SANDBOX → `verified_output_revision_id` (resolving its artifact — verified subject of `evaluate_publish_sandbox`); MATERIALIZE_PRODUCTION → sealed artifact; PUBLISH_PRODUCTION → the exact production OUTPUT REVISION (the loader resolves and verifies its materialization attempt). **Loaders NEVER return verdicts — availability and decisions belong to `action_decision` alone** (an unavailable-verdict loader would be a second authority). ALL `ActionRequestV1` call sites migrate: `build_sets.py`, `feature_execution.py`, the coordinator, workers. Pre-resource cards say "potentially available after selection" (projection, not decision).
- **R2 — execution context never touches logical identity.** Context lives in `PhysicalExecutionPlanV1`; formula reuse keys on the logical digest; new environment → new physical revision.
- **R3 — physical adoption: append-only, user-confirmed, ENVIRONMENT-SCOPED.** Chain root and CAS head scoped by `(selection_revision_id, execution_context_revision_id)` — a selection may hold a sandbox plan and a production plan simultaneously (round-9). POST names `physical_plan_revision_id` explicitly (expected_predecessor alone doesn't say what was chosen). DDL proves: plan and selection share the logical digest; per-scope single root (partial unique WHERE supersedes IS NULL); one successor per predecessor; CAS under concurrency; idempotency via semantic-fields-only content_hash (actor/time = provenance); every build-set member's adoption belongs to the BUILD environment and is compatible with the generation authorization's environment. Endpoints enforce read scope + record the authenticated actor.
- **R4 — no runtime observations without a runtime (round-9 corrected).** Preview EMITS runtime guards (the join-validation checks compiled into generated code); CI fixture validation PROVES the guards fire (a seeded fan-out fails the fixture run by name). NO `JoinValidationObservationV1` is written from CI fixtures as if it were evidence about user data — operational observations are DEFERRED to the future governed sandbox worker (chartered). Cards: *Preview rendered* / *Guards proven on platform fixtures* / (never) *Sandbox executable*.
- **R5 — synchronous identity, asynchronous telemetry.** The serving transaction synchronously persists the logical plan, considered-option binding, and activation facts (never fail-soft, never worker-dependent). Telemetry rides a NEW versioned result-outbox; the worker persists serialized results verbatim, never replans live work; 1121 (applied, immutable) keeps its legacy input lane; shadow planning = separate experiment lane excluded from live denominators. Demand once per occurrence via `demand_identity_hash`.
- **R6 — reuse platform codes.** Cardinality reason = existing `DIRECTIONAL_CARDINALITY_UNPROVEN`. `CARDINALITY_EVIDENCE_REQUIRED` = demand category only. `REALIZATION_ATTACHMENT_DEFECT` = ops alert.
- **R7 — one authoring subject, ever.** The 1103 identity ("one tuple, one hash, three uses") is THE authoring subject. Promotion (closed class: "LLM deterministic intent awaiting formula", the `feature_planning_contracts.py:248-252` seam) stores its evidence in a PROMOTION-EVIDENCE REVISION keyed by the existing subject + the `formula_draft_plan_binding` — **no `authoring_subject_revision` store exists anywhere in this plan** (round-9 killed the leftover); conceptual patterns stay blocked.
- **R8 — narrowed supersession, owned prerequisites.** This plan absorbs the remediation's fact-loader requirement + plan-envelope overlap; Tasks B0a/B0b OWN its preserved work (authoritative scope, target lineage, frozen method identity) in dependency order. P1 records the split in both documents.
- **R9 — logical identity = feature MEANING only (round-9 tightened).** The logical digest contains: canonical definition content hash/revision; operation; typed operand bindings + their governed semantic revisions; output grain (ordered key refs); SELECTED parameter (name, value) pairs; the logical relationship path; formula-policy identities. NOTHING else — `planning_request_hash` OUT (varies per hypothesis: two hypotheses choosing the same 90d feature share one identity), the parameter-spec-set hash OUT (a menu copy-edit or a new 180d option must not re-key a 90d feature). Provenance/evidence pins carry: hypothesis, planning request hash, chooser revision, display projection, full menu, ranking evidence. ONE chooser owner module (the two existing implementations consolidate).
- **R10 — temporal join safety, SPLIT BY IDENTITY LAYER (round-11: temporal strategy changes feature MEANING — "owner at transaction time" and "owner at August cutoff" are different features).** `LogicalTemporalSemanticsV1` lives IN the logical digest: `effective_time_strategy ∈ {as_of_event_time, as_of_cutoff, static_link}`, `knowledge_time_strategy ∈ {as_known_at_cutoff, labelled_restatement}` (`latest_correction` is BLOCKED for training/backtesting features — `KnowledgeTimeBasisV2`'s own doctrine: post-cutoff corrections are leakage; a restatement workflow must be explicitly labelled), driving-time role, interval inclusivity, unmatched-row semantics (exclude-vs-preserve changes values → LOGICAL). `PhysicalTemporalBindingV1` lives in the physical digest: valid_from/valid_to column refs (catalog-qualified), availability/knowledge-time column, source binding revision, snapshot selection. Changing logical semantics re-keys logical_digest; changing only column realization re-keys physical_digest. **B3c INTEGRATES the existing authorities — `DatasetTemporalPolicyRevisionV1` (`temporal_policy.py:162`), `KnowledgeTimeBasisV2` (`boundary_v2.py:182`), and the bridge realizations' effective-interval predicates — never a second temporal language.** Static-link eligibility only with declared immutability; AI-proposed links never default to static (`TEMPORAL_JOIN_POLICY_MISSING` until confirmed via B2b's surface). Registered code; Warn/Block×5. D6 pins the March-transaction/August-owner case.

## Identity payloads & lifecycle (the executable spec)

```
LogicalFeaturePlanV2 (per R9)                                  → logical_digest
PhysicalExecutionPlanV1: logical_digest, execution_context_revision_id (existing
  environment_id/ExecutionTier vocab), source_binding_revisions, per-segment {realization_revision,
  ordered column pairs, predicates, directional cardinality, realization content+dependency hashes,
  R10 temporal strategies + validity interval}, join_validation_policy_revision_id → physical_digest
RenderProfileV1 (round-10 slimmed, reusable): engine, compiler_version, renderer_version,
  template versions → render_digest
GenerationConfigurationV1 (round-10, per build): population/spine declaration, target mode/ref,
  cadence where it affects content, per-feature empty values, physical type policy, policy
  realization set, engine settings → generation_config_digest
MULTI-MEMBER BUILD IDENTITY (round-11: EXTENDS the platform's existing chain —
  `CompilationIdentityV2` already carries formula_content_hashes + ir_hashes, and generate_v2
  hashes the actually-rendered files; this composes with, never replaces, that protection):
  member_semantic_digest   = sha256(logical_digest | formula_content_hash)
  member_compile_digest    = sha256(member_semantic_digest | physical_digest |
                                    member_output_config | ir_hash | policy occurrence bindings)
  build_compilation_digest = sha256(target/spine | ORDERED member_compile_digests |
                                    generation_config_digest)
  artifact_content_digest  = hash of the ACTUALLY rendered files (generate_v2's discipline)
  sealed artifact          = build_compilation_digest + render_digest + artifact_content_digest
  member_output_config (TYPED, single owner — GenerationConfigurationV1 drops per-feature
  empty values): output name, empty-value policy, decimal/rounding policy, physical type,
  ordering. D3 pins the two-member order-reversal digest change.

DIGEST LIFECYCLE (round-9: a card CANNOT carry a composed digest — no environment/adoption/render
exists yet): card → selection → formula PRESERVE logical_digest; adoption → build set → IR
PRESERVE physical_digest + logical_digest; generation → rendered artifact INTRODUCES render_digest
+ composed_artifact_digest. Acceptance asserts each identity at ITS lifecycle boundary.

JoinValidationPolicyRevisionV1 (reusable semantics; round-9 split): null_key_behavior
  {exclude_row|refuse}, unmatched_row_behavior {preserve_left_null|exclude|refuse}, coverage
  {numerator=matched left rows, denominator=eligible left rows post-predicates, minimum},
  orientation (left=source), max_matches_per_left_row, snapshot-SELECTION rule (how a cutoff is
  chosen — never a concrete snapshot), policy_revision_id, content_hash over SEMANTIC fields only
  — declared_by/declared_at are PROVENANCE, excluded from content_hash (round-10, matching R3).
  FAN-OUT LAW: final-grain aggregate + max_matches > 1 → ALLOCATION_POLICY_REQUIRED unless a TYPED
  physical operator proves pre-aggregation/dedup/governed allocation. Concrete snapshots + measured
  counts live ONLY in the (future-worker) observation record.
```

## Identity & staleness law
Proposed→confirmed / strength / display-text: no rekey. Withdrawn / revoked: refuse. Superseding realization: refuse old pin; R3 adoption is the path. Join column/predicate/cardinality/temporal-strategy change: NEW physical identity. Environment: new physical revision only. Same selected parameters via different hypotheses: SAME logical identity. Menu edits: no rekey (R9).

## Disposition matrix (all six actions; three-part registrations; CI exhaustiveness green)
DIRECTIONAL_REALIZATION_MISSING, DIRECTIONAL_MAPPING_INCOMPLETE, EXECUTION_CONTEXT_MISSING, JOIN_COVERAGE_POLICY_MISSING, MAX_MATCH_POLICY_MISSING, TEMPORAL_JOIN_POLICY_MISSING, TRANSACTION_IDENTITY_NOT_UNIQUE → Warn | Block | Block | Block | Block | Block. `DIRECTIONAL_CARDINALITY_UNPROVEN` (EXISTING code, row restated for the implementer) → Warn | Warn | Warn | Warn | Block | Block. EXECUTION_SOURCE_COMPATIBILITY_UNPROVEN → Warn | Warn | Block | Block | Block | Block. **SANDBOX_EXECUTION_NOT_RELEASED (round-11 P0-7): deployment capability is a SERVER-OWNED fact folded by the SAME action service** — `action_available`'s "policy-available" and the run rail's separate switch-folds are TWO answers today; every route and rail consumes the one folded verdict; EXECUTE_SANDBOX refuses BEFORE queueing (work guaranteed to fail is never accepted). OPERAND_ROLE_UNRESOLVED, ALLOCATION_POLICY_REQUIRED (final-grain) → Block ×6. Join NULL/unmatched semantics: exclude-vs-preserve lives in LOGICAL identity (it changes values — R10); coverage minimums and max-match guards stay physical validation policy (`JOIN_NULL_POLICY_MISSING` refers to the physical-validation declaration of the logical choice). M:N source-grain intermediate = its own first-class logical plan/card.

## §V verified facts (nine rounds; T0 re-verifies ALL at then-current origin/main)
V1 bridges/projection (`bridge_projection.py:42-56,141,165,168`). V2 G3-at-compile + G2 masking (`governed_lens.py:36-67,580,648`; `need_metadata.py:84,108`; `declarations.py:716`). V3 realization readers + PRODUCTION-tier defaults (`assembly.py:404`; `bridge_store.py:125,776,782,795-800,900`) → sandbox-scoped rows required. V4 serving unreachable (`contract.py:776`; `gate1.py:1161` vs `:1308`). V5 generation lane blind to plans (`selection_formula_binding.py:32,41-42`; `restore_formula_v3.py:91`; `admission_v2.py:94-100`; `joins.py:920`; 1072:83 append-only no-supersession). V6 renderer unqualified keys (`render/project.py:348-350` vs `wiring.py:199`). V7 1120 CHECK closed; 1121 applied+immutable. V8 two authorities disagree live (`activation_policy.py:37,152` vs `action_dispositions.py:128,136`); caller-assembled facts diverge (`code_generation_jobs.py:272` vs `generation_lane.py:282`). V9 grain law: `posted_debit_amount` = ACCOUNT grain (`transaction_foundation.py:67-70`); B10 UOA refusal (`recipe_planning_lens.py:259`); ONLY reviewed expectation; **`posted_debit_transaction_count` = `_base_operands(with_direction=True)` ONLY — no row_selection, no txn-id operand, generic event_ts (:195-200)**; demotion seam (`feature_planning_contracts.py:248-252`); blueprint count→COUNT_ROWS. V10 `FEATUREGEN_MATERIALIZE_INVENTORY` off; activation flag + signed gate (`live_activation.py:56,242`); `PLAN_CONTRACT_VERSION` in vector; chooser address includes hypothesis (`param_choice.py:96`); two chooser modules. V11 remediation NO-GO Tasks 4/5/8 → B0a/B0b; **roles reach generation on BOTH routes** (`build_sets.py:166→363`; the jobs route's execution-parameters path into `request_code_generation_job`/coordinator ~:543 — T0 pins the exact field); registry: their 1121-1129 (1121 dead), 1117 owner unknown. V12 (round-9): the REAL run-to-code lane is `feature_runs.py:134 prepare_run_code` → `code_generation_coordinator._build` (:241 region) building from `selection_formula_binding` — blind to logical/physical/render identities; `evaluate_publish_sandbox(conn, *, verified_output_revision_id)` (`evaluate_execution.py:131`).

**Migration allocation (round-10: T0 assigns FINAL numbers in EXECUTION order — none of these is applied anywhere, so durable environments get monotonic history; the list below is the content inventory, not the numbering):** execution-context (A3) · demand vocabulary (A7) · three-layer persistence (B1) · binding chain + adoption + join-policy store (B2) · principal/data-scope revision (B0a — round-10: "no dependent DDL" was impossible; the revision table + its bindings to the generation request/job, action decision, and queue payload get their OWN migration) · B0b lineage/method constraints (after B1/B2's tables) · temporal-policy persistence (B3c) · promotion-evidence revision (B5) · fixture-evidence records (B6) · result outbox (C2). Reserve 1130-1139 as the block (+ B2a's realization-proposal store if it needs one); **T0 produces THE single authoritative content→number mapping and NO task text carries a hard-coded number until then** (round-11: all task-level numbers removed; the allocation list above is content inventory only).

---

# TASKS (each = one SDD dispatch, TDD, own commit; order is dependency order)

**T0 — verify + registries.** Re-verify V1-V12 + all nine rounds' citations; R1 call-site inventory (`ActionRequestV1` consumers); the jobs-route roles field; canonical `ActionV1` members; candidate-revision identity material; 1117 owner; registry reconciliation for 1130-1137; R8 split recorded. Ledger deltas; amend before dispatch. *No code.*

**P1 — parent amendment + supersession.** Stage-2 gate split (implementation now / activation behind signed evidence); S2-P4 = composed carrier with the R9/round-9 digest lifecycle; S2-P6 split (source compatibility blocks EXECUTE_SANDBOX, never GENERATE_PREVIEW); R8 split in both docs.

## Phase A — discovery
**A1 — authority consolidation, part 1.** The loader FRAMEWORK + `facts_for_author_formula` (1103 subject) + the legacy-rung adapters + disagreement-inventory regression tests + three-part code registrations + capability projection with pre-resource copy. (GENERATE_PREVIEW/EXECUTE/PUBLISH loaders complete in B3 once B1/B2 create the facts they load — round-9 ordering.)
**A2 — projection extension.** Full ordered endpoint tuples, link semantic revision, evidence split; drop `members[0]`; no global direction; staleness-row tests.
**A3 — execution context** (migration: per T0 mapping). Immutable revisions over existing vocab; `EXECUTION_CONTEXT_MISSING` registered.
**A4 — realization snapshot.** Constant-query batch extending `CompileBudget`; truncation cause persisted+disclosed.
**A4b — identity CONTRACTS, code-only.** The three dataclasses + canonicalization + digests per R9 (round-9 payload boundaries); lifecycle-stage digest tests; R9 identity pins (same params/different hypotheses → one digest; menu edit → no rekey).
**A5 — logical resolution without physical evidence** (consumes A4b; DECLARED identity change: PLANNER_VERSION bump, Stage-1 pins regenerated). Unrealized proposed link → logical plan for card/formula rungs; `grain_refs` derive from it; G3 preserved for physical rungs.
**A6 — G2 serving gate + fixtures.** `OPERAND_ROLE_UNRESOLVED` for every potentially-served option; account-grain fixture catalogs ("ops" transactions + "core" accounts, proposed `account_number↔acct_id` link); hypothesis "accounts with high posted debit outflows" (a LEVEL). B10 customer-UOA refusal pinned.
**A6b — the count recipe, structurally honest** (round-11: the DEFAULT is fixed NOW, executable). `posted_debit_transaction_count` gains: the structural debit row_selection; a transaction-id operand whose default behavior IS the implementation — uniqueness violated → `TRANSACTION_IDENTITY_NOT_UNIQUE` (registered code): preview may render its guard, the fixture/run FAILS, NO count is produced. COUNT_ROWS never consumes the id operand as a dedup — the operand exists to CHECK. Deduplication, if ever, arrives later as an immutable `TransactionDeduplicationPolicyRevision` {identity-key columns, survivor ordering, correction/reversal behavior, knowledge-time cutoff, tie behavior, refuse-when-no-unique-survivor} + a typed survivor OPERATOR before COUNT_ROWS — never a silent COUNT_DISTINCT substitution (chartered, not this scope). Window bound to the posting timestamp role; recipe REVISION bump; prior review invalidated. D2/D5 use THIS recipe for count cases (D5's count case asserts THE REFUSAL on the duplicate fixture, and the clean-fixture count value); monetary cases use `posted_debit_amount` — never mixed in one assertion.
**A7 — demand vocabulary** (migration: per T0 mapping) + one-writer law + satisfaction projection.

## Phase B — the pinned plan (round-9 dependency order)
**B0a — close client roles, BOTH routes, globally.** `GenerationIn.roles` + the jobs route's execution-parameters roles path removed for ALL builds; `IdentityEnvelope.role_claims` resolved server-side; a principal/data-scope REVISION persisted (its OWN migration per the allocation — an immutable role/data-scope snapshot bound to the generation request/job, the action decision, and the queue payload); the queue carries its binding id. **Worker recheck DEFINED (round-11 tightened):** frozen claims prove what WAS authorized; the worker additionally consults the CURRENT principal resolver (the platform's existing one — the same resolver the routes use; T0 pins its module path) to decide whether execution may still proceed — frozen-claims authorize, current-claims gate. Tests: both routes + a forged queue payload refuse; recheck consumes the frozen revision, never re-reads request headers.
**B1 — identity persistence** (migration: per T0 mapping) for the A4b contracts.
**B2 — total binding chain + adoption + join-policy store.** The five totality mechanisms (parent-carried composite FKs / deferred triggers); R3's environment-scoped adoption table; `JoinValidationPolicyRevisionV1` table. **Round-10: RELATIONAL agreement, never hash equality** — the chain considered option → logical binding → draft binding → selection binding → physical adoption → build-set member is proven by FKs; the existing `binding_plan_hash`/`planning_request_hash` become PROVENANCE pins stored beside the bindings (different payloads than `logical_digest` — comparing them for equality would be meaningless). Legacy rows pre-plan, refused for cross-catalog.
**B0b — lineage/method constraints** (migration: per T0 mapping, AFTER B1/B2's tables): target-reading agreement across jobs/selections/members; frozen strategy/review/method identity + worker no-recompute; each extended with the logical-plan reference.
**B2a — the provisional-realization producer (round-11 P0-1; before B2b).** Files: a new `overlay/upload/bridge_realization_proposal.py` + `bridge_store.record_realization_revision` becomes reachable from production code. The server-owned pipeline per the header: A2's ordered endpoint mappings → frozen physical source bindings → SANDBOX-scoped directional realization (cardinality UNKNOWN) → B3c temporal + B2b join policies attached → provisional physical plan → guards. Mapping-incomplete → `DIRECTIONAL_MAPPING_INCOMPLETE`, never a positional zip. Tests: an AI-proposed link with a complete mapping becomes a PROVISIONAL-previewable plan end to end; incomplete mapping refuses; the produced realization is sandbox-scoped and never enters the production executable reader.
**B2b — adoption + policy WORKFLOW** (round-9: the policy path must exist or D2 dead-ends). `GET /selections/{id}/candidate-execution-plans` (environment, join keys, predicates, cardinality, temporal strategies); `POST /selections/{id}/physical-plan-adoptions` {physical_plan_revision_id, expected_predecessor} CAS; join-policy server PROPOSAL + confirmation API + UI controls over the closed choices; policy binds to the adoption. Read-scope enforced; actor recorded.
**B3 — authoring + generation integration.** Logical digest into authoring request/audit/evidence pins/validation; legacy drafts refused for cross-catalog; `PinnedResolvedFeatureInputV3` through restore→admission→compile (pinned = sole authority; pinned-context revalidation; diagnostic-only fresh runs); COMPLETE the remaining R1 loaders (preview/build-set subject, sealed artifact, verified output revision, production subjects) and migrate all call sites.
**B3b — the run-spine coordinator carries the plan (round-9 P0-1, THE gap).** Files: `api/routes/feature_runs.py` (`prepare_run_code`), `materialize/code_generation_coordinator.py` (`_build` + `request_code_generation_job` path). The coordinator resolves, server-side, for EVERY selected member: the logical binding, the environment-matching physical adoption, the render profile + generation configuration — and builds ONLY from those combined bindings (minting the ordered `member_execution_digest`s → `build_input_digest`); a member lacking any of them refuses by name. **The direct code-generation route uses the SAME service** (owner-ratified ruling — one journey; retirement, if ever, = explicit retired-route response). Tests: the principal journey STARTS at the hypothesis/run endpoint; a confirmed cross-catalog plan survives `prepare-code` into the build set; the two-member order-reversal build-digest pin.
**B3c — temporal relationship realization (R10; round-10 NEW; before B4).** `TemporalJoinPolicyV1` contract + persistence (allocation) + server proposal + confirmation surface (B2b's actor rules) + compiler/IR/renderer as-of join operators + adversarial PIT tests (validity-interval join; post-cutoff change excluded; correction-after-cutoff honored per knowledge_time_strategy). AI-proposed links never default to static.
**B3d — the joined-attribute feature path (round-11 RULED: one exact semantic shape, no in-task alternatives).** A NEW recipe `posted_debit_amount_for_eligible_accounts` (never mutating `posted_debit_amount`). THE pipeline shape (pinned): transaction scan → transaction PIT filter → debit/reversal/currency filters → account dimension scan → account PIT/knowledge-time filter (B3c semantics) → governed N:1 temporal join → joined account-status predicate → aggregate by account. THE mechanism (pinned): a SEPARATELY SEALED logical-plan predicate — carried in `LogicalFeaturePlanV2`, cryptographically bound into `member_semantic_digest` beside the formula hash, compiled as a typed pre-aggregation join+predicate IR stage — NOT a Formula-V4 language change this scope (`schema_v3` stays sealed; `build_operator_graph_v2`'s joined-expression refusal stays for formulas; the PLAN owns the join). Full wire coverage: prompt, response schema, parser, canonicalizer, critic, admission, method identity, compiler, IR, renderer versioning — FakeLLM proves the flow, the schema/parser tests prove the wire protocol carries the new meaning. Formula method DECLARED: LLM-authored + spend + provenance. Before D3b.
**B4 — provisional lane with GUARDS (R4; round-10: no fake operational lifecycle).** Provisional assessment + reader + join IR + `compile_expression` wiring + renderer support; guards compiled into generated code from the bound policy. CI fixture proof = the guard FIRES and the fixture run FAILS BY NAME with no output rows — recorded ONLY as B6's artifact-bound fixture result. NO queue items, NO dashboard entries, NO terminalization claims from CI (a test harness never manufactures what looks like an operational run); the operational lifecycle arrives with the future governed validation worker (chartered).
**B5 — promotion machinery (R7).** Promotion-evidence revision (migration: per T0 mapping) keyed by the EXISTING subject + `formula_draft_plan_binding`; validator + API + spend via existing cost-confirmation + provider audit identity; `request_draft_for_candidate` consumes promoted subjects; FakeLLM end-to-end; conceptual patterns still refuse.
**B6 — artifact-bound fixture evidence** (migration: per T0 mapping). "Guards proven on platform fixtures" claims bind {artifact, digests, fixture-set version, result}; user-data claims impossible (R4).
**B7 — ONE chooser, promoted (R9).** Consolidate the two modules; serving path: bounded menus, ALL ordered parameters, audited hypothesis-conditioned call with deterministic fallback; chooser revision → provenance; values → logical digest; "90-day hypothesis never gets a 30-day feature" pinned.

## Phase C — rendering + serving
**C1 — one dataset-key dialect** (`catalog::schema.table` through raw keys, dataset names, node inputs, catalog YAML, spine validation, gate manifests, source-binding validation; RENDERER_VERSION bump inside `RenderProfileV1` with the round-9 output-affecting declarations; two-catalog same-name distinct; wrong-catalog spine REFUSES).
**C2 — serving under one verdict + synchronous identity** (result-outbox migration: per T0 mapping). Activation object resolved once + pinned; branch restructure (engine+governed additive; anchor rule); serving ceiling; synchronous logical-plan/binding/activation persistence; first-serve ranking; `GOVERNED_SERVING_POLICY_VERSION` + `PLAN_CONTRACT_VERSION` bump same commit; byte-identity inactive.
**C3 — the serving merge + identity contract (round-11: was empty; now the spec).** Files: `contract/gate1.py` (the merge), `semantic_option_decision.py`, `api.ts` types, ranking module. Rules: ONE logical feature = ONE card — ranking dedupes by `logical_digest` so a feature never consumes several result slots; an engine card and a governed card with the same logical meaning MERGE under the governed variant when activation is on (engine provenance noted), else the engine card serves alone (byte-identical when inactive); two governed variants sharing a logical meaning → one card, best-ranked variant primary, alternates listed with their variant ids; draft/confirm submits `(option_id, governed_variant_id)`; history displays the variant identity; cap/truncation decisions disclosed on the response. Tests: twin-merge (engine+governed one card, provenance intact); same-logical two-variant dedup; slot-count pin; disclosure pin; the decision row keys by variant while the column stays canonical (V5's law).
**C4 — the card experience.** Four-section home (Ready to generate / Formula available / Discoveries requiring setup / Not suitable); named blockers + remedies; provenance; provisional + temporal-policy states; execution-truth states per R4; production governance-not-released; backend-driven states only; adoption + policy confirmation UI; accessibility/responsive tests.

## Phase D — journeys (start at the hypothesis/run endpoint — never low-level build APIs; CI fixture validation only)
**D1** no realization → card+formula; preview `DIRECTIONAL_REALIZATION_MISSING`; demand once. **D2** unknown cardinality (A6b count recipe; LLM+FakeLLM+spend per A6b; policy confirmed via B2b) → provisional preview; guards PROVEN on fixtures (pass + seeded-fan-out fail); reason `DIRECTIONAL_CARDINALITY_UNPROVEN`. **D3** exact N:1 (realization + environment-scoped adoption) → full preview; asserts logical_digest at card/selection/formula, +physical_digest at build/IR, +render/composed at artifact — each at ITS boundary; IR shows the cross-catalog join step, two catalog-qualified inputs, bridge-gate node, ordered grain tuple. **D3b (round-9 P1; round-10 made implementable)** a feature whose MEANING uses both catalogs: the B3d recipe `posted_debit_amount_for_eligible_accounts` (its NEW joined-attribute operator or governed pre-filtered spine, its B3c temporal status policy, its declared LLM-authored formula with spend) — consuming a genuine point-in-time operand from the second catalog. Runs ONLY after B3c+B3d land. **D4** known M:N → final-grain `ALLOCATION_POLICY_REQUIRED`; source-grain intermediate first-class; demand recorded. **D5** value journeys, SPLIT: (a) COUNT (A6b recipe): duplicate txn ids counted once (or refused, per A6b's pinned choice) — concrete input rows + expected integers in the task's fixture; late posting included iff posting-ts in window at cutoff; post-cutoff excluded. (b) MONETARY (`posted_debit_amount`): reversals excluded; debit-direction rule; closed/dormant included when transacting (true semantics); joint M:N → source-grain value + final-grain refusal; lookback-vs-horizon separate assertions. NAMED REFUSALS: multi-currency-FX; reference-enrichment; missing bridge → realization demand. **D6** temporal safety (R10): ownership changes AFTER the cutoff → the join uses the validity interval; the leak fixture refuses/excludes by name. **D7** LLM-origin: deterministic-awaiting-formula → B5 promotion → formula → bindings → build set → preview.

## Phase E — operator rail (explicit user go): migrations backend-first; telemetry + worker; `FEATUREGEN_MATERIALIZE_INVENTORY` + mappings + SANDBOX-scoped realization seeding; satisfaction projection; count-recipe/AML expectation reviews (optional upgrades); targeted-cohort activation ONLY after P1's amended gate; SME thresholds + wave-1 gate BROAD. **Programme milestone (round-10, post-sandbox-worker): empirical feature quality — coverage, null rate, stability over time, leakage, redundancy vs same-catalog features, incremental validation performance, segment fairness/population drift. First-card proves representability; this milestone proves VALUE.**

# Not in scope (chartered): M:N allocation policies; full dimension-enrichment joins (D3b's entity-attribute filter is the boundary); full S2-P6; public sandbox execution + operational join observations (the governed validation worker); federated execution; propose-bridge surface; promotion beyond R7's class; per-dimension features.

# Execution
SDD; T0 first; ledger `.superpowers/sdd/<plan-basename>/progress.md`; final whole-branch review (most capable model); every deploy/flag = explicit user go.
