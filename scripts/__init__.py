"""Operator scripts that are importable, so they can be tested.

The ledger verifier is the reason this package exists: a check that guards deploys and is itself
untested is the same shape of problem as a runbook step nobody runs.
"""
