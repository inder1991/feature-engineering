{
  "case_id": "over_deep_filter_is_invalid",
  "operation": "sum",
  "adversarial": true,
  "delivery": "provider",
  "note": "§J classification: over-deep filter -> INVALID. MAX_FILTER_DEPTH=4 is a §A SEMANTIC rule, not a JSON-Schema bound, so this proposal is shape-valid, reaches validate_semantics, and is REJECTED. The five nested NOTs put the predicate at depth 6.",
  "intent": {
    "name": "deeply_filtered_amount_90d",
    "hypothesis": "Transaction value under a deeply-conditioned filter over a trailing 90-day window predicts churn.",
    "target_entity": "customer",
    "target_grain_keys": ["authored::public.txns.cif_id"]
  },
  "proposal": {
    "formula_schema_version": 1,
    "operation_grammar_version": 1,
    "canonicalization_version": 1,
    "grain": {"entity": "customer", "keys": ["authored::public.txns.cif_id"]},
    "body": {
      "final_operation": "identity",
      "expr": {
        "aggregation": "sum",
        "operand": "authored::public.txns.txn_amt",
        "source_relation": {"table_ref": "authored::public.txns"},
        "filter": {
          "kind": "bool", "op": "not", "children": [
            {"kind": "bool", "op": "not", "children": [
              {"kind": "bool", "op": "not", "children": [
                {"kind": "bool", "op": "not", "children": [
                  {"kind": "bool", "op": "not", "children": [
                    {"kind": "predicate", "op": "is_not_null",
                     "left": "authored::public.txns.status_cd"}
                  ]}
                ]}
              ]}
            ]}
          ]
        },
        "window": {
          "event_time_ref": "authored::public.txns.txn_dt",
          "basis": "trailing", "length": 90, "unit": "day",
          "start_inclusive": "inclusive", "end_inclusive": "exclusive",
          "timezone": "Asia/Dubai", "empty_window": "null", "null_input": "ignore"
        }
      }
    },
    "parameters": [],
    "decimal": {"precision": 38, "scale": 6, "rounding": "half_even", "overflow": "error"},
    "expected_output": null
  },
  "critic_findings": [],
  "expected_disposition": "REJECTED",
  "expected_axes": {
    "structural_status": "invalid_formula"
  },
  "expected_output_policy": null,
  "expected_operands": [],
  "expects_blocking_critic": false,
  "canonical_json": null,
  "expected_formula_hash": null
}
