# GENERATED by featuregen.materialize.render — do not edit.

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "sandbox_feature_cif_daily"
version = "1.0"
# Pinned, not bounded: every version below is what the target environment was CAPTURED
# running (§0). A range would let the artifact install something the cluster does not run.
requires-python = "==3.11.9"
dependencies = [
    "kedro==0.19.9",
    "kedro-datasets==4.1.0",
    "pyspark==3.5.1",
]

[tool.kedro]
package_name = "sandbox_feature_cif_daily"
project_name = "sandbox_feature_cif_daily"
kedro_init_version = "0.19.9"
source_dir = "src"

[tool.setuptools.packages.find]
where = ["src"]
namespaces = false
