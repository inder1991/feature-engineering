# GENERATED by featuregen.materialize.render — do not edit.
#
# EVERY storage location the generated project touches is here and nowhere else. A path or
# a table name written into node source could not be reviewed with the rest of the
# environment's configuration, and nothing reading that source could tell it apart from a
# computed value.
#
# The only tables named below are the group's authorized physical read set (§1.3), the
# declared entity population (§4), this generation's staging area, and the ONE publication
# target §10.1 binds the group to.
#
# Layers (§7): raw -> intermediate -> primary -> feature_staging -> feature.

# ── raw ───────────────────────────────────────────────────────────────────

raw_banking__customers:
  # governed source, read-only: banking.customers
  type: spark.SparkHiveDataset
  database: "banking"
  table: "customers"
  write_mode: "errorifexists"
  metadata:
    kedro-viz:
      layer: "raw"

raw_banking__transactions:
  # governed source, read-only: banking.transactions
  type: spark.SparkHiveDataset
  database: "banking"
  table: "transactions"
  write_mode: "errorifexists"
  metadata:
    kedro-viz:
      layer: "raw"

# ── intermediate ──────────────────────────────────────────────────────────

intermediate_cross_border_value_ratio_90d__body_denominator:
  # point-in-time projection for cross_border_value_ratio_90d / body.denominator (§8)
  type: spark.SparkDataset
  filepath: "${runtime_params:staging_root}/intermediate/cross_border_value_ratio_90d/body_denominator"
  file_format: "parquet"
  save_args:
    mode: "errorifexists"
  metadata:
    kedro-viz:
      layer: "intermediate"

intermediate_cross_border_value_ratio_90d__body_numerator:
  # point-in-time projection for cross_border_value_ratio_90d / body.numerator (§8)
  type: spark.SparkDataset
  filepath: "${runtime_params:staging_root}/intermediate/cross_border_value_ratio_90d/body_numerator"
  file_format: "parquet"
  save_args:
    mode: "errorifexists"
  metadata:
    kedro-viz:
      layer: "intermediate"

intermediate_distinct_merchant_count_90d__body_expr:
  # point-in-time projection for distinct_merchant_count_90d / body.expr (§8)
  type: spark.SparkDataset
  filepath: "${runtime_params:staging_root}/intermediate/distinct_merchant_count_90d/body_expr"
  file_format: "parquet"
  save_args:
    mode: "errorifexists"
  metadata:
    kedro-viz:
      layer: "intermediate"

intermediate_total_debit_amount_30d__body_expr:
  # point-in-time projection for total_debit_amount_30d / body.expr (§8)
  type: spark.SparkDataset
  filepath: "${runtime_params:staging_root}/intermediate/total_debit_amount_30d/body_expr"
  file_format: "parquet"
  save_args:
    mode: "errorifexists"
  metadata:
    kedro-viz:
      layer: "intermediate"

# ── primary ───────────────────────────────────────────────────────────────

primary_spine:
  # the declared entity population (§4) — one row per (keys…, business_dt)
  type: spark.SparkDataset
  filepath: "${runtime_params:staging_root}/primary/spine"
  file_format: "parquet"
  save_args:
    mode: "errorifexists"
  metadata:
    kedro-viz:
      layer: "primary"

# ── feature_staging ───────────────────────────────────────────────────────

feature_staging_cross_border_value_ratio_90d:
  # cross_border_value_ratio_90d: (keys…, business_dt, cross_border_value_ratio_90d) only — no system columns (§10.2)
  type: spark.SparkDataset
  filepath: "${runtime_params:staging_root}/feature_staging/cross_border_value_ratio_90d/data"
  file_format: "parquet"
  save_args:
    mode: "errorifexists"
  metadata:
    kedro-viz:
      layer: "feature_staging"

feature_staging_distinct_merchant_count_90d:
  # distinct_merchant_count_90d: (keys…, business_dt, distinct_merchant_count_90d) only — no system columns (§10.2)
  type: spark.SparkDataset
  filepath: "${runtime_params:staging_root}/feature_staging/distinct_merchant_count_90d/data"
  file_format: "parquet"
  save_args:
    mode: "errorifexists"
  metadata:
    kedro-viz:
      layer: "feature_staging"

feature_staging_total_debit_amount_30d:
  # total_debit_amount_30d: (keys…, business_dt, total_debit_amount_30d) only — no system columns (§10.2)
  type: spark.SparkDataset
  filepath: "${runtime_params:staging_root}/feature_staging/total_debit_amount_30d/data"
  file_format: "parquet"
  save_args:
    mode: "errorifexists"
  metadata:
    kedro-viz:
      layer: "feature_staging"

feature_staging_manifest_cross_border_value_ratio_90d:
  # StagingManifestV1 for cross_border_value_ratio_90d: §9's completeness evidence
  type: json.JSONDataset
  filepath: "${runtime_params:staging_root}/feature_staging/cross_border_value_ratio_90d/manifest.json"
  metadata:
    kedro-viz:
      layer: "feature_staging"

feature_staging_manifest_distinct_merchant_count_90d:
  # StagingManifestV1 for distinct_merchant_count_90d: §9's completeness evidence
  type: json.JSONDataset
  filepath: "${runtime_params:staging_root}/feature_staging/distinct_merchant_count_90d/manifest.json"
  metadata:
    kedro-viz:
      layer: "feature_staging"

feature_staging_manifest_total_debit_amount_30d:
  # StagingManifestV1 for total_debit_amount_30d: §9's completeness evidence
  type: json.JSONDataset
  filepath: "${runtime_params:staging_root}/feature_staging/total_debit_amount_30d/manifest.json"
  metadata:
    kedro-viz:
      layer: "feature_staging"

feature_staging_assembled:
  # the assembled group — §9's gates run on THIS, and only a group that passes every one of them reaches the publication target
  type: spark.SparkDataset
  filepath: "${runtime_params:staging_root}/feature_staging/_assembled"
  file_format: "parquet"
  save_args:
    mode: "errorifexists"
  metadata:
    kedro-viz:
      layer: "feature_staging"

# ── feature ───────────────────────────────────────────────────────────────

feature_cif_daily:
  # the publication target, derived from the sandbox binding: sandbox_feature.cif_daily
  type: spark.SparkHiveDataset
  database: "sandbox_feature"
  table: "cif_daily"
  write_mode: "errorifexists"
  metadata:
    kedro-viz:
      layer: "feature"
