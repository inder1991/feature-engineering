"""BR-11..16 — the V2 recipe packs, one module per family. Each pack authors atomic
RecipeDefinitionV2 entries (one output each), declares its governed policy references in the
BR-10 ``<kind>:<name>`` form, and states every legacy replacement explicitly. The packs are the
prose-admission cleanup: a recipe that once apologized for a missing concept now binds the
governed concept BR-10 added."""
