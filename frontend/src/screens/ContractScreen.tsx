import type { ReactElement } from 'react'

// The governed two-gate feature-contract flow (brief -> considered set -> confirm). Task 3 fills this in.
export function ContractScreen(): ReactElement {
  return (
    <section aria-labelledby="contract-heading">
      <h2 id="contract-heading">Govern a feature</h2>
    </section>
  )
}
