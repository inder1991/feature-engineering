# Local Claude Code settings (machine-specific)
.claude/settings.local.json

# OS cruft
.DS_Store

# Subagent-driven-development scratch (ledger)
.superpowers/

# Python build / cache artifacts
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.venv/
.ruff_cache/
.mypy_cache/

# build artifacts
build/

# local worktrees (frontend session, etc.)
.claude/worktrees/
deploy/kind/.llm-key

# Local L0 validation environment (PySpark + Kedro; not a project dependency)
.venv-l0/

# Real bank glossary exports — LOCAL ONLY, never committed.
# These carry production schema, business definitions and PII/sensitivity classifications.
# They live outside the repo (~/Downloads); these patterns are a backstop for the case where
# one is copied in by accident. Never `git add -A` while one is present.
*Column_Mapping*.csv
FTR_Column_Mapping*.csv
CIB_Customer_Column_Mapping.csv
